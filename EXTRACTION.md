# Ultima V Extraction Inventory

Master tracking checklist for the cleanroom specification phase. This file maps
the original DOS/GOG asset and code inventory to the public specification docs
in this repository.

Last updated: 2026-09-05.

## Status Legend

| Status | Meaning |
|--------|---------|
| Complete | Covered by cleanroom prose in this repository at v1 depth. |
| Verified slice | Covered by cleanroom prose and exercised by the first playable verification slice. |
| Partial | Covered enough for v1 orientation, with visible parity details still called out in the doc. |
| Deferred | Intentionally outside the v1 visible-MVP spec or waiting on more reverse engineering. |
| Out of scope | Original DOS/runtime implementation detail that a modern engine is not expected to reproduce. |

## V1 Baseline

The v1 visible-MVP baseline covers the IBM PC DOS gameplay contract: startup,
intro, character creation, save/load, overworld, town interiors, dungeon mode,
combat, conversation, NPC schedules/pathfinding, shops, time, visibility,
lighting, weather, karma, magic, vehicles, active objects, animation, endgame,
and the primary asset/data formats needed by those systems.

The public spec set currently contains:

- 51 system specs in `systems/`.
- 26 file-format specs in `formats/`.
- 9 cross-cutting catalogs in `catalogs/`.

The specs are cleanroom prose. They may cite private analysis notes by path, but
they must not include source code, assembly excerpts, copied decompiler output,
or raw copyrighted data dumps.

## Issue #184 - the unexplained `SAVED.GAM` bytes (2026-09-02)

Seven save-image bytes the engine could not account for after a load-and-save
round trip are now published, and six reversals came with them (`RETRACTIONS.md`
rows R338-R343). `0x02DE` keeps its twelve-hour **value** rule but loses the word
*display* - nothing renders it, and the ambient-audio tick decays it toward zero,
which is why a save reads `00` there. `0x02DF`/`0x02E0` are the cached Trammel and
Felucca phase digits that natural-moongate transit reads, not scratch
(`formats/saved-gam.md` Section 5.1). `0x02FF` is the ambient light level, now
carrying its value rule and its skip sentinel in the save-format table. `0x03B2` is
the resident-Shadowlord latch, reset on every town-family entry. Wind at `0x02EC`
is **not** rerolled at load or entry - the banner is a reprint - and drifts only
through the per-idle-tick roll `systems/weather.md` Section 2.1 now scopes and
quantifies. Active-object record byte `+6` carries no facing. And the whole NPC
runtime family lives inside the save image: a town-family load enters in the
preserving mode that rebuilds none of it, so an engine that persists only the
active-object table resumes an empty town. A sibling correction fell out of the
same work: the moon-phase status strip is refreshed at every overworld and
town-family scene entry as well as from the cleanup's hour-change hook, so
`systems/moons.md`'s "exactly one caller" cadence and the below-surface
unreachability it rested on are withdrawn (R343).

## Issue #188 - the three compositor residuals (2026-09-03)

`systems/visibility.md`'s three unresolved compositor questions are closed and a
new Section 8.5 owns the answers. **The grid-versus-map asymmetry is real and
prescriptive**: the default helper reads the scene's map at the actor's absolute
coordinate, the single-sprite-family seated branch reads the visibility grid at
the actor's projected viewport cell, and one actor takes both in one frame. They
differ in buffer, index space, value domain, failure mode and position in the
pass, and shipped roster content exercises both halves. **Combat has no
compositor of its own** (R365): there is exactly one, combat enters it through
the shared per-frame world tick, and combat's differences are five named skips
inside the post-pass rather than a skipped scene - so Section 11's old model was
wrong and Section 8.1's was right. **The compositor's neighbour probe is
unbounded in an arena** and bounded by substitution in a location: at an arena
edge it reads outside the arena record (residue, unreachable in shipped
content, recommended engine behaviour is *no match*), while on a location floor
it takes `town-mode.md` Section 15's southeast-corner `(31,31)` substitution
first-hand. That substitution has **one origin and more consumers than the two
this answer contrasts**: the shared world-tile accessor produces it, the
compositor consumes it by calling that accessor, and the town movement predicate
consumes it second-hand through the grid - and only where nothing later
overwrote the cell. The P-Push out-of-range sample (`systems/commands.md`
Section 8.2) and the tavern serving probe (`systems/shops.md`) take the same
substitution by the same rule, so the pair above is a contrast, not a census. A sibling correction
fell out of the same work (R366): this document's "out-of-bounds sentinel"
framing is withdrawn - there is no dedicated sentinel storage and no fixed
sentinel tile.

## Issue #190 - six residual save-state questions (2026-09-03)

The residuals #184 left open in `systems/moons.md`, `systems/time.md`,
`formats/saved-gam.md` and `systems/shops.md` are all closed. **The moon-strip
painter's below-surface erase arm is live**, on four routes: Ararat, the
Underworld plane, the Blackthorn audience's first repaint, and a basement floor
in any of the four locations that own one. `systems/moons.md` Section 3 now
carries the caller census as a table with its scan scope stated, and it answers
the in-place question in both directions: every in-place return to the overworld
repaints, **and so does every in-place floor change inside a location** — the
staircase, the trapdoor and the NPC-death reload all run the same floor loader a
fresh entry runs. The moongate warp is the one caller whose in-place repaint
depends on the origin-and-destination pair, and the ordinary overworld-to-town
warp repaints through neither of its exits. The "two command handlers" turn out
to be two arms of a **single** command, `H` (Hole up); the audience cutscene
repaints **once**, not twice, with its real post-cutscene refresh coming from
the caller (R375); and the arrest jail relocation refreshes on the normal arm.
`systems/time.md` Section 11 names the ambient sub-tick residues as **zero and
four**, tested before the counter advances, on a counter that is free-running
from program start and outside the save image. `systems/moons.md` Section 2.2
withdraws the instruction to treat an out-of-range day-of-month as a save-data
error (R376): the original performs an unchecked table read, nothing validates
the field, and the bytes it yields reach moongate destination selection —
the section now separates that descriptive rule from the prescriptive
clamp-at-load advice. `systems/shops.md` Section 8.4 publishes the six per-inn
bed cells as a table, with the one-tile eastward step the party takes on waking
and the three early exits that skip it.

## Issue #191 - the controlled monster's turn (2026-09-03)

The three-way conflict between `systems/combat.md` Section 16.1, `RETRACTIONS.md`
R354 and R364 is resolved **in R354 and R364's favour**: a monster carrying the
controlled/charmed bit is **prompted**, not synthesized. Section 16.1's
"still synthesizes an automatic action" clause was the last surviving instance of
the framing R353 and R364 withdrew elsewhere, and it is withdrawn (R377). Six
reversals land with the answer. The one an engine should re-audit first is R378:
the **fixed magic-strike branch is party-slot-only** - it sits on the automatic
driver, a monster-side slot never reaches it, and on the prompted path the same
distance-one number is the targeting cursor's silent range clamp rather than a
refusal that can waste a turn. Section 8's shape-A gate reads the **party-side
bit, not liveness** (R381), which is what makes seven letters refuse for a
controlled monster and what makes `Z` prompt one for a character; Section 8.1's
banner paragraph had the status early-outs on the wrong side of the banner and
described one re-prompt shape where there are two (R380). And Section 6.3's
party-death row loses "death audio played" and gains the right write order plus
the stats redraw (R379): there is **no combat party-death cue**, which is now a
listed silence boundary in `systems/audio.md` Section 9, with the world tick's
in-arena ambient tone named as the thing a recorded session might mistake for
one. The sixth reversal is the smallest and the easiest to miss: `Aim! ` prints
on the melee arm only, so `systems/magic.md` Section 8 and
`catalogs/spell-list.md`'s Conjure/Swarm/Summon note were wrong to publish the `Attack-` / `Aim! ` pair for
every creature those spells place - Summon's own Daemon is a non-melee-reach
class (R382).

## 2026-09-04 - Reconciliation pass and the open-verification index

A pass with no new tracing reconciled the handoff documents against the tree
and the ledger. Nothing behavioural changed, so no `RETRACTIONS.md` row was
added. What did change:

- **`OPEN-QUESTIONS.md`** now indexes every statement this repository publishes
  as open, unverified, inferred or disputed, each with what would settle it
  (an owner decision, a live capture, a further static trace, or scope). It is
  the forward-looking complement of `RETRACTIONS.md`.
- **`scripts/check_crossrefs.py`** joins the contamination checker: every
  document path and every "Section N" reference in the tree must resolve,
  `RETRACTIONS.md` ids must be contiguous, and the document counts stated in
  this file must match the tree. Its first run found the counts here two
  behind the tree and three references in the historical log to specs that
  were never created; all fixed.
- The statements that this repository is private, made here and in
  `NEXT-STEPS.md`, were stale and are corrected: the specification is public
  and the shipped-text decision above is live.
- The private correction ledger was walked entry by entry against the spec
  tree. Every correction dated 2026-08-22 and 2026-08-26 whose target is a
  public document is present in that document; the ledger's own propagation
  markers lagged and were brought up to date on the private side. The one
  ledger item still open against the public tree is the tile-catalogue
  range re-derivation, which needs the shipped description table and is
  queued as the first `trace` item in `OPEN-QUESTIONS.md`.

## 2026-09-04 - The tile catalogue's partition re-derived from the description table

The one private-ledger item still open against the public tree is closed.
`LOOK2.DAT` was decoded end to end (512 offsets, 216 distinct string offsets,
197 distinct texts, two placeholders) and `catalogs/tile-catalog.md` Sections 2
and 3 now publish per-run class tables for both halves in place of the
fourteen-contiguous-class hypothesis, which was wrong almost everywhere above
index 15: the terrain half's "NPC", "vehicle", "path", "wall" and "furniture"
bands were scenery, markers, castle pieces and interleaved props, and the actor
half runs items, vehicles, occupied furniture and people before the combat-class
bands. Seven reversals, R383-R389, including the NPC, item, vehicle and effect
range sentences of Sections 8-11. Three combat-class bands turn out not to be
creatures (the regalia, the fields, the whirlpool), which the bestiary already
half-recorded as gaps. Labels in the new tables are paraphrased; the shipped
strings themselves stay in the private decode.

## 2026-09-05 - Every static-trace residual closed against the shipped files

With the shipped files available again, the twenty-four `trace` items that
`OPEN-QUESTIONS.md` listed on 2026-09-04 were worked through in two days and
that section is now empty. The behavioural results, in the documents that own
them: the Glass Sword **does** consume its readied slot (`systems/combat.md`
12, R390 - the one reversal of the sweep); a controlled party actor's fixed
strike hands the primitive a real weapon id, so its to-hit never collapses
(`combat.md` 11); a landed Corpser hit sets the dragged-under bit and the
victim's turns roll `1..30` against its base step to surface with the
regurgitated line (`combat.md` 8.1, 11.1); a controlled monster's `Z` opens the
shared party picker and its non-melee `A` fires its class's own ranged attack
with the player aiming (`combat.md` 8, 16.1); strength, dexterity and
intelligence are all seeded at 15 (`chargen.md` 6, 7); the visibility carve's
threshold is the caller's light value (`visibility.md` 3, 14) and the scratch
behind an arena record is never rewritten mid-fight (`visibility.md` 8.5); the
inn bed cells are two-cell beds with Jhelom's upstairs, no location can descend
below its lowest page except through Stonegate's scripted ring, and Iolo's Hut's
cells are walkable (`shops.md` 8.4, `location-dat.md`, `active-objects.md` 10);
the Shadowlord readout is the telescope's night sky (`quest-graph.md`,
`view.md` 4.2); the whirlpool marker's animation script and the general script
opcodes are published (`active-objects.md` 8); both three-unit blip sites emit
both pitches (`audio.md` 8.6); every endgame helper resolves to a documented
primitive (`endgame.md` 12); and underworld object records are added by the
same plane-selected writers as surface ones (`ool.md` 11). The Tandy driver's
subtitle-ignition transition was also documented for the first time
(`timing.md` 7.6, `display-driver-mode.md` 6). What remains open is listed in
`OPEN-QUESTIONS.md` sections 2 and 4 only - live-capture items and scope
deferrals.

## Status Of The Engine Issue Queue

As of 2026-09-04 the `cleak/u5-spec` issue queue is **empty**: one hundred and
ninety-one issues have been filed by the clean engine and all are closed, the
last eleven (#181-#191) between 2026-09-02 and 2026-09-03 with their answers
summarised in the dated sections above. The earlier milestone stands as
recorded: as of 2026-08-22 all eighty-three issues then filed were closed. Seventy-two of them were re-audited
in a single pass that verified each thread's answer against the spec working
tree and against the shipped binaries; twenty-four were confirmed already
resolved and forty-eight were corrected, with the earlier wrong answers
explicitly retracted in the thread. The remaining eleven (the intro, gameplay
screen, floor-page, dungeon presentation, endgame and lighting issues raised
from black-box runtime observation) were traced and published the same day.

Two classes of error recurred often enough to be worth stating as process
lessons for whoever works here next. First, several earlier answers were
**anchored wrongly in the resident data image**, producing plausible tables that
did not survive re-derivation; any table published from a byte walk should be
cross-checked against an independent consumer of the same data. Second, a
correction that lands in the normative doc is not finished: the same withdrawn
wording usually survives in two or three sibling docs that were never opened.
Grep the whole spec for the retracted phrasing before treating a correction as
propagated.

## Shipped-Text Policy — Decided 2026-09-04

**Decision (repository owner, 2026-09-04): keep the four items as published.**
Each has a functional argument - parser input the seal predicate cannot be
specified without, a decode table a renderer cannot work without, and layout
and test vectors for the two prose items - and the total is small. The
question below is kept as the record of what was weighed; it is no longer
open, and the `owner` row in `OPEN-QUESTIONS.md` is closed.


A semantic cleanroom audit on 2026-08-22 read all eighty-four documents against
the boundary rules and found the spec substantially clean: one clear violation
(since rewritten), no decompiled code, no assembly, no private address tables,
and no reproduction of the game's writing in the great majority of docs. Several
docs state an explicit policy of *not* transcribing authored content and
summarise it instead.

What the audit did surface is a question the repository owner should settle
rather than an agent: **how much shipped text this spec should reproduce.**
The great bulk of what is published is short interface text — command echoes,
prompts, refusals, menu labels, place and item names — which a faithful
reimplementation cannot omit and which totals roughly 1,000–1,200 words across
the whole spec. Four items sit outside that justification:

| Item | Doc | Approx. size | Character |
|---|---|---|---|
| The Avatarhood certificate (body, ceremonial closing title, closing report) | `systems/endgame.md` §9.2–9.4 | ~60 words | Authored ceremonial prose — the clearest case of the game's *writing* rather than its interface |
| The two step-6 inline doorway narration lines | `systems/intro.md` §10.1 | ~17 words | Authored narrative sentences from the story sequence |
| The eight Words of Power and their dungeon mapping | `systems/commands.md` §11.1 | 8 words | Puzzle solutions the player is meant to discover; also parser input the seal predicate cannot be specified without |
| The 128-slot common-word token vocabulary | `catalogs/common-word-dictionary.md` | 118 words | A decode table — a `.TLK`/`SHOPPE.DAT` renderer cannot produce correct output without it |

None of these is large, and each has a defensible functional argument. But the
certificate and the doorway lines are the game's authored prose rather than its
interface. The private analysis workspace stays private, but **this
specification repository is now public**, so the decision is live rather than
hypothetical (it is the one `owner` item in `OPEN-QUESTIONS.md`): the options
are to keep them, to replace them with structural
descriptions plus placeholder text, or to describe their layout and let the
implementer read the strings from the user's own copy of the game data at
runtime — which is the approach several other specs here already take for
`SHOPPE.DAT` barks and `KARMA.DAT` verdict paragraphs.

## The Correction Pipeline Itself Was Audited

The private correction ledger in `../u5-decomp/` is how findings reach this
spec: an entry states a finding, names a target doc, is propagated, and is
struck through as landed. On
2026-08-22 one entry was found to be *itself wrong* and to have overwritten a
correct spec value — a regression disguised as a fix. That prompted an audit of
all eighty-five entries against the shipped binaries rather than against the
notes they cite.

**Thirty-two entries were wrong or partially wrong. Four had already propagated
their error into this spec** and have been repaired: the runic-incantation table
(a dense forty-eight-entry pointer-addressed set, not a sparse forty-seven-entry
run), the save path's per-plane object-file reads (which do happen, and which an
earlier pass on the same day had wrongly withdrawn), NPC AI mode five (fully
implemented, merely unused in shipped data, not a reserved dispatcher slot), and
a Lord British roster slot that does not exist in any `.NPC` file.

The dominant failure mode is worth naming, because it defeats ordinary review:
**a false premise, not a misread instruction.** A table anchor two entries late.
A scene byte assumed to mean combat when it is Doom, the eighth dungeon. A
routine assumed to have one caller when it has five. Each produced a plausible,
internally consistent, wrong answer, and each survived multiple reviews because
reviewers checked the reasoning rather than the premise it rested on. When
auditing a claim here, check what it assumes before checking what it concludes.

## Measured Residual Error Rate

On 2026-08-22, after the correction passes, an unbiased sample was taken to
answer "how much is still wrong?" rather than to hunt for more bugs. Claims were
selected by a positional rule (every third numbered section of each doc, first
checkable assertion) with no regard to how suspicious they looked, and each was
verified directly against the shipped binaries rather than against the private
notes — the notes and the spec share an ancestor and can be wrong together. A
second agent then audited each sample for selection bias and rubber-stamping.

**Result: 138 claims sampled, 131 correct, 5 wrong, 2 unverifiable — a residual
error rate of roughly 3.6%,** of which 3 would have broken an engine
implementing them. All five have been fixed. Treat the figure as a
per-section-cluster rate rather than a per-assertion rate: some sampled units
were a single sentence, others were whole tables that proved exhaustively
correct (the 48-row equipment table matched the shipped arrays in all 288 cells).

Two things are worth carrying forward from the measurement:

**A correction source can itself be the defect.** The worst of the five — the
spell scene allow-mask with its dungeon and combat bits transposed — was a
*regression*. Git history shows the spec had the mapping right, and a later pass
overwrote it from a `CORRECTIONS.md` entry whose own Finding line was wrong.
That entry was then struck through as landed, which made the regression look
like a completed fix. The root cause was an annotation in a private note that
assumed the combat scene byte is `0x28`, which is in fact Doom, the eighth
dungeon. Propagating a correction is not safe merely because it is labelled a
correction.

**Verification against the ancestor proves nothing.** Several errors survived
multiple review passes because reviewers checked the spec against the private
notes, which agreed with it. Only re-derivation from the shipped binaries
distinguishes "the spec matches our notes" from "the spec matches the game".

**A repair pass can breach a boundary the original error did not.** On
2026-08-23 the clean engine reported that one document sourced its claims by
private note filename. The sweep found the pattern repo-wide: **1056 citations
across 77 files, exposing 440 distinct private entry points**, because a note
filename encodes a routine's load offset and its private label. That is a
private address table distributed through public prose, and it accumulated
without anyone noticing because it arrived a few citations at a time in a shape
that reads like diligent sourcing. No single line looked like a table.

It was introduced by an earlier **name-audit repair pass** - a pass whose whole
purpose was to improve rigour, and which cited its work as it went. That pass
has now produced three distinct classes of damage: wrong names introduced while
fixing wrong names, a spec regression from a retracted correction, and this. Its
defect record is worse than the errors it was cleaning up. **Audit repair passes
against the same rules as the thing they repair** - they arrive framed as the
fix rather than as a change, and so escape review.

All citations are now condensed to their directory, which is permitted
provenance and keeps the useful signal. Five historical issue comments carrying
the same leak were redacted; GitHub comment and issue text is clean.

**A mechanical check now exists**: `scripts/check_contamination.py`, seven rules
covering offset-bearing citations, segment- and binary-relative addresses,
assembly with and without operands, and raw byte dumps. It carries a self-test
and runs it before every scan, because a checker reporting "clean" from broken
rules is the failure it exists to prevent. That self-test immediately caught a
rule silently matching nothing after a mangled escape, and a gap where
operandless instructions were invisible.

## Names are unreviewed assertions

A name is a claim that no review checks, because reviewers read a name as a
label and audit the code beneath it. False claims in a name therefore live
*above* the level being audited, and they are load-bearing: a later reader
reasons from the name whenever the behaviour is unobvious.

Two measured instances. A clean-side constant named for a moongate animator's
daytime threshold made four claims - that an animator existed, that it belonged
to moongates, that it gated on daytime, and that the gate was a threshold.
**Three were false**; the real mechanism belonged to a different subsystem and
its polarity was inverted. On this side, a field named for a combat magic effect
timer asserted an owner and a mechanism, **both false** - it is the moon-gate
presence counter, and nothing in combat reads it. An entire invented spec
section was built on top of that name.

Of 636 private routine names, 53 were found wrong and 16 had reached published
prose. A wrong name is more durable than a wrong sentence: prose gets re-read,
names get trusted.

**Three mechanical tests**, all cheap and none dependent on the analyst being
right:

| Test | Catches | Why it is mechanical |
|---|---|---|
| Does anything read this? | Contracts that are not real | A reference count, not an impression |
| Is this byte inside the save window? | Real contracts at the wrong scope | One subtraction |
| Does this name assert something nobody checked? | Wrong owner, polarity, mechanism, units | Names decompose into separate claims |

The second settles every lifetime question for resident state, and it overturned
an answer given confidently twice from control flow alone. The third is the
weakest - it needs a person to enumerate the claims - but has the widest blast
radius, because a name propagates into every document that cites it.

## Known V1 Deferrals

These items are intentionally not treated as blockers for the v1 visible-MVP
baseline, but they remain important for exact parity:

- Alternate-driver exact rendering and non-load-bearing EGA helper slots. The
  EGA dispatch ABI, back-buffer plane layout, visible page-zero policy,
  front-buffer tile/glyph paths, the packed-to-planar preparation entry, and
  major effect entries are now covered in `systems/display-driver-abi.md` (the
  standalone bitmaps are one-bit sub-image lists, `formats/bit.md`); exact CGA,
  Hercules, and Tandy conversion details remain parity follow-up work.
- Music/audio playback for distributions that ship external music resources.
  The analyzed clean DOS baseline at `C:\Games\U5-Clean` contains no `.XMI`
  files, so XMIDI is not a v1 asset-format requirement for this baseline.
- Exact CGA/Hercules/Tandy rendering differences.
- Return-to-View resident helper internals are closed for EGA/Tandy. The map
  strips, command stream, exact open/close row-splice rasters, single-cell
  256-position permutation, 31 preview-tick/input checkpoints, opaque
  zero-colour rule, and non-transactional abort state are public. The formerly
  alleged short fixed wait is a speaker effect and is not a timing gap.
- Several per-table enumerations called out in their docs: full tile-id
  verification, remaining unnamed monster class flag bits, some NPC
  sprite-class role labels (AI-byte behaviour values and NPC floor-link marker
  IDs are fixed), less-visible combat side-trait row publication, and a few
  combat metadata bits. Combat reward/reconciliation ownership is now a
  negative boundary: framer non-merge, temporary drop-marker shape, ordinary
  attack/spell experience credit, original trigger-slot reconciliation, and the
  absence of combat-exit gold/karma/victory-bonus writes are public.

## 1. Code Modules

The public specs are organized by behavior rather than by binary module. A
single overlay may feed several specs, and a single spec may combine resident
and overlay behavior.

### Resident Core

| File | Role | Status | Spec docs |
|------|------|--------|-----------|
| `ULTIMA.EXE` | Boot entry, machine/display probing, main loop, command dispatch, text I/O, active objects, visibility, overlay loading, overlay call ABI, shared movement/passability helpers, combat framing, stats-panel redraw including the resident miniature tile-glyph path, save/load helpers, display dispatch contract, separate resident disk-insert prompt and disk-error recovery handler, shared counter arithmetic, gameplay PRNG, and DOS runtime profile | Complete | `systems/boot.md`, `systems/main-loop.md`, `systems/overlay-abi.md`, `systems/movement.md`, `systems/commands.md`, `systems/input.md`, `systems/text-output.md`, `systems/stats-panel.md`, `systems/active-objects.md`, `systems/visibility.md`, `systems/time.md`, `systems/save-load.md`, `systems/display-driver.md`, `systems/display-driver-abi.md`, `systems/disk-prompt.md`, `systems/prng.md`, `systems/stat-arithmetic.md`, `systems/timing.md`, `systems/runtime.md`, `formats/data-ovl.md` |
| `ULTIMA5.COM` | Legacy launcher assumption | Complete | `systems/launcher.md` documents that the analyzed clean DOS baseline starts from `ULTIMA.EXE`; no canonical `ULTIMA5.COM` launcher is required for v1. |

### Game-Mode And Feature Overlays

| File | Role | Status | Spec docs |
|------|------|--------|-----------|
| `MAINOUT.OVL` | Outdoor/underworld loop and per-turn epilogue | Complete | `systems/overworld.md`, `systems/encounters.md`, `systems/weather.md`, `systems/time.md`; scene entry, per-tick redraw/chunk refresh, movement commit, special underfoot latch, waterfall-family surface falls chain, active-object per-turn handling, encounter probe inputs, ship-combat trigger, save/object-overlay ownership, mode hooks, per-turn cleanup linkage, saved-slot natural moongate live-tile refresh/waning, live `0xDC` moongate shimmer/entry handling, the midnight first-ten-minutes shrine/urn branch, the cached-moon-glyph saved-slot warp path, and the traced `Q`/`T`/horse-carpet pendulum gates are covered, with encounter probe and random-spawn behavior complete at outdoor-loop depth, no additional outdoor underworld-to-surface writer identified in the current writer census, completed-disassembly and binary direct-write scans finding no additional static writer to the saved Moonstone-slot coordinates, and shipped-map scans finding no static natural-moongate tile cells in `BRIT.DAT`. Tile naming, opaque timing/transport values, and chunk-substitution labels are catalog or QA work. |
| `OUTSUBS.OVL` | Outdoor chunking, chunk-loader live-buffer substitutions, entry checks, confirmed surface falls trigger, actor/object setup, and Lord British camp-event level-up | Complete | `systems/overworld.md`, `systems/rest-and-camp.md`, `systems/active-objects.md`, `formats/brit-dat.md`, `formats/under-dat.md`; sparse/dense chunk loading, four-chunk window refresh, live-buffer substitutions, filename/plane selection, town-entry checks, confirmed falls transition, per-plane active-object setup, and the outdoor-camp Lord British level-up service are covered at overlay-helper contract depth. Broader outdoor transition/catalog, timing-tag, transport-marker, and chunk-substitution naming outliers remain with `systems/overworld.md`, tile/catalog work, or presentation QA rather than `OUTSUBS.OVL` helper ownership. |
| `TOWN.OVL` | Town/keep interior setup and loop | Complete | `systems/town-mode.md`, `systems/npc-schedules.md`, `formats/location-dat.md`; entry/load, marker harvest, dawn/dusk rewrite, deterministic cosmetic terrain variation, per-scene NPC activation/death mask, resident Shadowlord installation plus the row-four coordinate bypass exposed by Yew-jail re-entry (the earlier phantom-player NPC attach is withdrawn), movement/exit/stair handling including the town-family exit thresholds and facing-sensitive `0xC4..0xC7` town stairs (tile `0x59` is the telescope Look trigger, not an exit threshold), town Open/Attack hooks, Lord British basement chord, Stonegate entry presentation producers plus chair/underfoot effects, town alarm / forced-pursuit / forced-flight schedule rewrite / arrest cleanup, the live NPC-conflict handoff into ordinary terrain combat and the post-fight town reload, schedule tick integration, NPC sprite linkage, free-roaming object movement, active-object ownership, entry-mode preservation behavior and traced caller census, authored secret-room mechanism delegation, and save/load entry reconstruction are covered at behavioral-contract depth. Stonegate presentation-asset parity, authored secret-location inventory, and rare nested script-return parity remain presentation/data/empirical work rather than `TOWN.OVL` loop blockers. |
| `DUNGEON.OVL` | First-person dungeon loop | Complete | `systems/dungeon-mode.md`, `formats/dungeon-dat.md`; command parser, movement and turning, active-monster collision, electric-field force-step presentation, post-action monster engagement, K-Klimb prompt/apply, exact `0x60` pit surface-reset path, attack-forward combat entry, surface-reset exits, pit-chain off-bottom scene-clear boundary, room triggers, room-clear demotion, and core turn-loop/rendering are covered. |
| `DNGLOOK.OVL` | Dungeon look/view commands | Complete | `systems/view.md`, `systems/dungeon-mode.md`, `systems/visibility.md`; L-Look class descriptions and fountain effect, V-View flood/minimap painter, minimap glyph class ownership, room view/layout paint, room-clear bitmap helpers, cleared-room marker demotion, room NPC setup, and view init/teardown are covered. |
| `COMBAT.OVL` | Combat mode, actor rounds, target selection | Complete | `systems/combat.md`, `systems/encounters.md`, `catalogs/monster-bestiary.md`; initiative and combat-entry PRNG order (**issue #178, 2026-08-31**: party base step is the raw dexterity byte with no floor or adjustment and is written once at seating; the monster speed variation reverts rather than clamps; the round loop's per-actor skip is a restraint guard on the stocks and manacles, **not** a wall/walkability guard; the loop-entry prologue runs once per encounter and its first action is a world tick; placement performs no terrain validation; there is **no** bump attack, and `A` opens a per-weapon targeting cursor with an adjacent-attacker interference abort - the earlier wall-skip, step-or-attack and "announce on Attack" readings are withdrawn in `systems/combat.md` and indexed in `RETRACTIONS.md`), core loop/framer/death reward unit, ordinary attack/spell experience credit, target-picker filters/fallback including Doom and Shadow Lord suppression-filter exceptions, slot-to-group faction resolution including the shipped traitor-template roster override and player-name exclusion, ordinary AI action selection (**corrected, issue #185**: the driver calls the shared primitives directly and enters no command parser - R353), shared weapon/spell damage-row split, item-id keyed weapon range/effect rows, range-gated melee versus ranged/projectile routing, shared to-hit formula, teleport-capable movement and ordinary movement fallback helpers, monster poison/status attack gating, ranged/effect branch selection and magic-immune gating, dual-use monster range/damage-selector and payload side-table semantics with source-free row values, the combat-only cursor/marker tail of the shared viewport rasterizer including exact EGA/Tandy geometry, colours, overwrite order and repaint-based erasure (**corrected 2026-08-23**: an earlier revision listed a "post-round terrain/effect sweep" here; that pass is not combat-scoped, is not per-round, and dispatches no cell effects - `systems/combat.md` carries the withdrawal), framer non-merge of temporary drop markers, absence of combat-exit gold/karma/victory-bonus writes, caller-side original trigger-slot reconciliation, absence of ammunition/thrown/glass item mutation across the traced combat attack stack and separate traced equipment consumers, Gargoyle/Gazer special-death branch assignment, boss-style vanish-on-death row assignments, decoded class-flag trait row assignments for poison/status attack, teleport-capable movement, turnable attacks including the Mage non-monster boundary, and monster-turn specials, and the opaque policy for component bits without independent behavioral consumers are covered. The ordinary melee/ranged to-hit and damage contract is now complete in both directions (**issue #183, 2026-09-02**: the score adds the defender's rating and subtracts the attacker's, the draw is the shared skewed `1..30` combat roll so per-score hit percentages are published, the defender term is always the per-actor combat weight (Dexterity for party, jittered class speed for monsters), a monster's attack value is its class byte used **flat with no roll**, the defence term is a `1..rating` roll on both sides and is skipped with no PRNG draw when the rating is zero, one attack per monster activation against zero to three per party turn, and an automatic-driver party attacker takes a fixed score of 15; reversals R334-R337). Attack-outcome narration is now complete for both directions (**issue #185, 2026-09-03**, `systems/combat.md` Section 11.1): an ordinary hostile monster's melee miss prints and sounds nothing, every result line names the target, a zero-or-negative result prints `grazed!` rather than a miss, the wound grading is monster-target only, a monster's ranged miss narrates a full hit whenever its scatter lands on an occupant, and a controlled monster is driven from the player's prompt rather than by AI; reversals R352-R355). Fourteen residual gaps are closed (**issue #187, 2026-09-03**): the aim marker's arena coordinate is the targeting cursor's own cell and the cursor owns it; the turn banner's colon is followed by a newline, so `Attack-` starts a new row after both the banner and the multi-item item-name line; the automatic round walk has **no** pacing of its own and no call to any resident delay helper, with the perceived cadence coming from blocking audio; the attacker's remembered previous target lives on the attacker's own record, is written only on a confirm that found an occupant, is cleared only by a confirmed empty cell on the ranged arm, and does not survive the encounter; the hazard tier's "leave-combat flag" is a stats-panel refresh request; the Gazer/top-tier effect is **sleep** and replaces damage; the "cast-like branch" is the Gremlin food theft gated on a non-empty party food supply; the teleport arm's chance, encirclement bypass, cell probe and draw budget are published; both class side tables are dense forty-eight-row arrays with selector one as the **melee** sentinel and two consumers that disagree above it; the tile-restoration pair's driver operand set is the mode value alone with the dungeon two-way-ladder cell as its only setter; the arena-centre special is live on the dungeon-room route, draw-free, and overwrites the centre terrain cell; the per-slot band is the incoming-attacker map and the "spell cast this round" flag does not exist; the instant-kill sentinel is reachable from the **monster** arm and has three producers; and the to-hit draw is fresh per attempt over a fixed helm/weapon-hand/shield-hand walk, with a parity caveat that the cursor itself consumes world-tick draws. Reversals R356-R364. |
| `COMSUBS.OVL` | Shared combat/spell helpers | Complete | `systems/combat.md`, `systems/magic.md` |
| `TALK.OVL` | NPC conversation runtime | Complete | `systems/conversation.md`, `systems/quest-flags.md`, `formats/tlk.md`, `catalogs/quest-graph.md`; fixed-window `.TLK` blob load and cap edge, opening description/greeting preamble, fixed reserved-keyword scan, ordinary per-NPC keyword scan, profanity/default rebuke plus bounded pause/timing loop and negative direct-karma-write boundary, three-digit gold-payment debit plus toll-milestone scalar selector path with no direct per-virtue standing write, JOIN/ASK-WHO control-byte boundary, protected-run print-mask behavior, label-record/scoped-prompt byte contract including inner empty-input behavior, reserved-word suppression, and label-response stop-versus-NUL returns, per-scene TALK branch flags including out-of-range zero-mask behavior, generic transient action flags, common-counter/Grapple/carpet/skull-key and Spyglass/Sextant/Black Badge action-letter effects, final conversation cleanup sentinel predicate, stolen-action warning glissando, random gold fallback, and shared town/conversation sentinel producer/cross-use with the shop surcharge gate are covered. |
| `NPC.OVL` | NPC runtime state, schedules, pathfinding | Complete | `systems/npc-schedules.md`, `formats/npc.md`, `catalogs/npc-roster.md`; roster block loading, one-based scene-to-file/sub-map selection with scoped restoration, per-entry runtime initialization, hour-to-waypoint selection, town-mode/rest-loop per-turn walker invocation, schedule boundary detection, eight-state movement classifier, flood-fill workspace and queue replay, dynamic obstacle radius, floor-link marker handling, hidden-sprite mask, active-object sprite linkage, and persistence omission are covered. Role-label and low-visibility helper polish belongs to roster/presentation validation, not the scheduler/pathfinding contract. |
| `SHOPPES.OVL`, `SHOPPES2.OVL`, `SHOPPES3.OVL` | Guilds, reagents, arms, healer, sage/tavern interactive flows, horse-trader sale placement, shipwrights, inn, horse-trader sale, companion pickup/dropoff | Complete | `systems/shops.md`, `formats/shoppe-dat.md`, `catalogs/item-list.md`; core shop flows, text rendering, Talk-entry shop dispatch/caller context, shipped `.NPC` shop-trigger values, karma non-modulation, horse-trader sale placement (adjacent-cell probe in south/north/east/west order, accepted placement tiles, horse active-object write, no display-stock row), per-shop vendor names and corrected `#`/`$` substitution sources, mounted-party shop entry gate, tavern provision branch (Intelligence-adjusted per-unit price, twenty-five servings per pack, per-unit pay loop, six-record quote pool, table-scraps outcome), arms stock-call literal pool, horse-trader Talk purchase, shipwright Talk sale trigger, shipwright active-object placement handoff, shipwright Frigate/Skiff payload semantics and duplicate-purchase edges, arms `B` buy stock indexing, arms `S` sell-back inventory browser, arms buy/sell Intelligence formulas, zero-price suppression, post-transaction random surcharge and shared town/conversation sentinel gate, reagent compact-menu availability, tavern drink round and Blue Boar fixed-choice price tables, tavern/meal-counter provision purchase and partial-affordability behavior, sage topic/fee/rumour substitution flow and input boundary, shipwright pending-action ownership, and inn registry marker/clear/death-source/month-counter behavior are covered; the earlier food-merchant `F`/`S` interpretation is superseded by the shipwright trace, no standalone provisions merchant is modeled for the analyzed baseline, and residual equipment-stat/catalog publication belongs to item/data-table specs rather than the shop overlays. |
| `INTRO.OVL` | Menu, title, intro slides, saved-game load | Complete | `systems/intro.md`, `systems/launcher.md`, `systems/save-load.md`, `formats/story-dat.md`, `formats/bit.md`, `formats/tiles.md` |
| `ENDGAME.OVL` | Endgame narrative sequence and terminal certificate | Complete | `systems/endgame.md`, `formats/end-dat.md`, `formats/endmsg-dat.md`; terminal sequence, normal overlay-export entry, statically resolved dungeon-room/post-combat cleanup callers, stock Doom final-room absorption handoff route, overlay-owned Lord-British-styled two-prompt confirmation dialogue, final-answer-plus-box-flag branch semantics, plain `.DAT` resource reads, fixed six-window `END.DAT` final narrative presentation, endgame graphics/font resource ownership, refusal-tableau random local wander, certificate date/elapsed-time calculation, and intentional no-return scroll terminal loop are covered at terminal-state contract depth. Exact character-animation pose mapping, display-helper taxonomy, and resource-slot/panel parity remain presentation QA rather than ENDGAME state-progression blockers. |
| `FLAMES.OVL` | Screen-preservation scratch helper historically named like a flame overlay | Complete | `systems/animation.md`, `systems/intro.md`; title/menu flame-style animation is display-driver-owned, and this file is not a separate gameplay animation system in v1. |
| `BLCKTHRN.OVL` | Blackthorn audience/rescue/challenge scenes | Complete | `systems/blackthorn.md`, with links to `systems/combat.md`, `systems/encounters.md`, `systems/conversation.md`, `systems/quest-flags.md`, `systems/karma.md`, `formats/location-dat.md`, `formats/miscmsg-dat.md`, and `formats/karma-dat.md`; audience/capture setup including the town-side captive-scene arrest-handler entry predicate, the shrine-selection scan over the shrine ruin flags (corrected 2026-08-22 from "already-jailed skips") and the town arrest-path entry (no separate caller-provided death-route marker), transient conversation-signal boundary, cutscene actor reuse, exact two-guard versus seated-Blackthorn slot roles, local byte-script VM state, actor-indexed movement semantics, the five traced script beats, exact quiet-pause and redraw semantics (including withdrawal of the alleged output-byte and screen-clear commands), terrain/actor tile identities and cell positions, direct 256-pixel cell reveals, rescue flash/dissolve boundaries and deterministic observable vectors, the four-prompt single-shrine mantra interrogation with its shrine desecration, moral-standing debit and irreversible companion execution, exact live prompt/answer mapping for Honesty/Ahm, Compassion/Mu, Valour/Ra, and Justice/Beh, response matching, failure punishment, rescue/refuge restoration, rescue `KARMA.DAT` five-band verdict selector, zero-only Food safety grant, shared moral-standing floor, absence of captive-counter/context storage, scene handoffs, and exact town/overworld/dungeon entry predicates are covered through pixel-level cinematic contract depth. |
| `CMDS.OVL` | A-Z world command handlers | Complete | `systems/main-loop.md`, `systems/commands.md`, `systems/overworld.md`, `systems/town-mode.md`, `systems/doors-and-z-transitions.md`, `systems/vehicles.md`, `systems/magic.md`; N-New Order whole-record active-party swap semantics are covered, including cancel paths, slot-zero refusal, same-slot self-swap turn consumption, and party-size non-mutation. X-Xit is covered as current-cell vehicle parking plus horse/carpet/skiff/ship transfer branches. M-command coverage now separates ordinary CMDS reagent mixing from CAST2's live shrine/urn entry handler; the CMDS `0x1202` mantra-style meditate branch belongs to the unreachable Yell-era leftover path, not an alternate M-command route. Y-Yell is covered as ship sail toggle, free-text prompt, Shadowlord-name route, Word-of-Power recognized-word rumble/flash feedback before the location test, live-tile transmutation plus visibility dirtying, unreachable mantra leftover boundary, and non-contractual per-word scratch-marker handling. H-Hole-up's town bed hours sub-handler is now corrected as a single-digit target-hour loop with the original subtract-23 wrap edge, one bounded NPC schedule/world-tick burst, repeated ten-minute cleanup calls, thrown-out revalidation, and temporary Good/Sleep cleanup; the resident overworld/dungeon rest-with-watch path now covers duration input, optional watch prompt, Good-status watcher validation, and dungeon rest/camp alternate handoff. No CMDS-owned Yell persistence gap remains. |
| `SJOG.OVL` | Search/Jimmy/Open/Get command family | Complete | `systems/commands.md`, `systems/containers.md`, `systems/doors-and-z-transitions.md`, `systems/overworld.md`, `systems/town-mode.md`, `systems/dungeon-mode.md`, `catalogs/item-list.md`; Search now covers surface object/slot-treasure/live-tile/Moonstone/reagent/fixed-table lookup routing, per-object trap-tier threshold narration, dungeon light-gated high-nibble feature classification, exact chest trap-tier narration, pit-family secret/bomb Search bytes, and flavour/wall visit-local rewrites. Jimmy now covers ordinary door/visible-chest/NPC rolls, NPC failed-key consumption, per-map object chest broken-lock state, and dungeon chest formulas. |
| `ZSTATS.OVL` | Character/inventory status display and R-Ready equipment flow | Complete | `systems/inventory.md`, `systems/text-output.md`, `formats/saved-gam.md`, `catalogs/item-list.md`, `catalogs/spell-list.md`; Z-stats character selection, stats/equipment page families, page navigation, inventory band browsing and row-prefix rendering, equipment slot order, equipment class tags, R-Ready picker flow, combat `R` routing through the same ready cascade with an active-actor selection shortcut, already-readied unequip flow, ranged-weapon ammunition readiness gates, burden-versus-Strength refusal, hand-occupancy gates, the Ring of Invisibility/Ring of Regeneration equip-time vanish check, carried-equipment counter mutations, Amulet/Turning's combat-passive boundary, and the separate equipment-weight helper's non-enforcing boundary are covered. No ZSTATS-owned R-Ready atomic-swap, displaced-equipment, or status-page routing parity gap remains; residual item-use, catalog, combat-consumer, and save-format gaps are owned by their respective specs. |
| `FONT.OVL` | Proportional text, chargen, and Return-to-View helper runtime | Complete | `systems/chargen.md`, `systems/text-output.md`, `systems/intro.md`, `formats/font-pcs.md`; paragraph layout now covers NUL termination, proportional width advances, space lookahead wrapping, hard newlines, underscore soft-hyphen breaks, and caller-owned brace/page pacing. Character creation covers the seed/commit path, rejection-sampled virtue picker, A/B tournament, corrected principle stat deltas, fixed Avatar class, seed-preserved start fields, and the questionnaire dexterity floor of 15 (**issue #178, 2026-08-31**: the dexterity tally starts from the seed record's value rather than from zero, so a chargen Avatar cannot have single-digit dexterity; the parallel STR/INT zero-start statements are flagged unverified in `systems/chargen.md` Section 7). Return-to-View covers the intro-local bytecode runtime, map-strip loader, per-frame active-object animation bridge, preview tile renderer, and screen save/restore bracket. |
| `LOOKOBJ.OVL` | Look-at-object/inspection text and surface view overlays | Complete | `systems/view.md`, `systems/conversation.md`, `systems/dungeon-mode.md`, `formats/look2-dat.md`, `formats/signs-dat.md`, `catalogs/tile-catalog.md`; L-Look master routing, LOOK2 terrain/object-domain split, object descriptions, clock/shrine/dungeon-entrance appenders, tile `0x59` routing to the night-sky renderer (the former Britannia-map attribution is withdrawn) with catalog-label reconciliation kept separate, overworld fountain presentation-only drinking with exact incapacitated/cancel gates, wishing-well keyword/gate flow plus same-object horse-family spawn semantics, fixed Yew wanted-poster coordinate branch, SIGNS.DAT record interpreter and coordinate order, death-vision Intelligence roll branches, Britannia chunk-map overlay, 32-by-32 local V-View modal overlay, party marker, tile-id to V-View visual-class mapping, and per-class V-View renderers are covered at gameplay-command depth. Pixel-perfect V-View presentation parity - per-class glyph placement, source-bank selection, border restoration timing, modal palette variants, and screenshot confirmation - remains visual QA rather than overlay-command ownership. |
| `CAST.OVL`, `CAST2.OVL` | Spell casting/effects, shrine meditation, Codex urn reading, save game | Complete | `systems/magic.md`, `systems/karma.md`, `systems/save-load.md`, `catalogs/spell-list.md`; player spell dispatcher order, parser, recipes, scene masks, major handler families, active-target attack-wrapper damage, Tremor's table-wide damage/reward path, Protection/Quickness/Mass Charm/Negate Magic active-effect consumers, shared active-effect counter aging, exact regalia effect codes and permanent duration sentinel, paired-byte U-Use install/remove semantics, Negate Time `T`/10 countdown semantics, combat C-Cast interference-source writer/revalidation/clear/persistence lifecycle, Blackthorn's-Castle/Stonegate absorption pre-gates, Clone's paired-slot capacity behavior and no separate per-spell duration counter, Conjure's weighted animal selector plus independent `0..10` X/Y placement attempts, Swarm's target-cell retry shape, Summon's eight independent random placement probes (**corrected 2026-08-23**: earlier revisions said "ordered eight-cell placement ring"; that is withdrawn, and `catalogs/spell-list.md` has carried the corrected reading since), low-circle Awaken/Cure status gates, selected-member Heal exact random-delta shape, Great Heal HP and combat-active refusal semantics, Locate/sextant coordinate formatting, ordinary world-door Open/Unlock helper semantics, Dispel Field dungeon live-cell and combat active-object removal semantics, resurrection status/HP/mana/experience/level rebuild semantics, combat field marker/contact/lifetime semantics, Poison/Sleep field-contact status gates, Fire/Energy field damage inputs, directed wind/sleep cone geometry and friendly-fire behavior, shrine/urn M-command split, CAST2 shrine/urn active-object pattern separation from party C-Cast summons, summon/team-control bit separation from the flee bit, Codex-read bit stamping, M-Mix wrong-recipe reagent loss plus shared trap-effect handoff, save-game writer ownership, and monster special separation from the player spell table are covered. Equipment counter semantics are delegated to `systems/inventory.md`; ordinary combat AI is delegated to `systems/combat.md`. |

### Display Drivers

| File | Role | Status | Spec docs |
|------|------|--------|-----------|
| `EGA.DRV` | 16-colour IBM PC display backend | Complete | Public rendering contract, dispatch ABI, mode-init/planar-buffer/palette mechanics, back-buffer plane layout, visible page-zero policy, full 38-slot dispatch surface including the corrected front↔back swap entry, the right-text-panel scroll, the asset-segment preparation entry, the transparency-mask blit trampoline, the LFSR-driven dissolve visit-order contract, and the title/animation/combat-exit restoration paths are covered at the EGA/v1 compatibility depth by `systems/display-driver.md`, `systems/display-driver-abi.md`, `systems/display-driver-mode.md`, `formats/tiles.md`, `formats/bit.md`, and font specs. The VGA-on-EGA-mode pathway is also clarified here: no separate VGA driver shipped, so VGA hardware runs this same EGA driver in the same 320-by-200, 16-colour mode. Remaining open items are exact alternate-mode asset byte coverage for the tile-palette helper, alternate-driver conversion (CGA/Hercules/Tandy), and pixel-perfect visual-parity QA. |
| `CGA.DRV`, `HER.DRV`, `T1K.DRV` | Alternate historical display backends | Out of scope | Modern v1 targets EGA-compatible visual assets; alternate hardware parity is deferred/out of scope. |

### Sound, DOS Runtime, And Installer Files

| Files | Role | Status | Notes |
|-------|------|--------|-------|
| Resident, overlay, and display-driver PC-speaker paths | Procedural sound effects, mute behavior, and presentation timing | Complete | `systems/audio.md` publishes the single-channel model, calibrated mute invariant, exact tone/glissando/rumble/envelope contracts, nine shared spell/potion/wind variants, priority trigger boundaries, named effect inventory, random-stream ownership, and explicit silence boundary. |
| PLINK86/MS C runtime code inside the shipped DOS executable | Overlay chaining, integer helper routines, fixed-capacity runtime model, and startup calibration | Complete | `systems/overlay-abi.md`, `systems/runtime.md`, `systems/timing.md`, and `systems/stat-arithmetic.md` cover the public compatibility contract for the analyzed DOS baseline without requiring modern engines to reproduce the private binary runtime. The overlay ABI now separates reachable entry ownership from byte-loader-harness-only descriptor/export details, and the shared arithmetic spec now treats the module-level helper-call census and public caller-family inventory as the complete shared-helper contract; exact per-field caps remain caller-owned spec work. |
| Miles/AdLib/Sound Blaster/MT-32/MIDI driver files | Historical audio backend | Out of scope | These files are not present in the analyzed clean DOS baseline. |
| `.XMI` music tracks | XMIDI music resources | Out of scope for this baseline | No `.XMI` files are present in `C:\Games\U5-Clean`; add a future XMIDI format spec only if a different target distribution ships them. |
| Causeway/DOS extender/config/installer files | Runtime and packaging support | Out of scope | Not needed by a modern cleanroom engine. |

## 2. Data Formats

### Maps And Combat Arenas

| File(s) | Contents | Status | Spec doc |
|---------|----------|--------|----------|
| `BRIT.DAT` | Britannia overworld chunks | Complete | `formats/brit-dat.md`; sparse chunk-index loading, all-water synthesis, coordinate wrapping, static terrain ownership, and runtime live-buffer substitution cases are covered. Remaining transition-coordinate, encounter, tile-catalog, and mutation-audit work belongs to runtime/catalog specs rather than the sparse map-file layout. |
| `UNDER.DAT` | Underworld chunks | Complete | `formats/under-dat.md`; dense direct chunk ordering, fixed file size, coordinate wrapping, static terrain ownership, and shared runtime live-buffer substitution cases are covered. Remaining ascent/dungeon/scripted transition, encounter, tile-catalog, and mutation-audit work belongs to runtime/catalog specs rather than the dense map-file layout. |
| `DATA.OVL` | Resident data image, labels, lookup tables, string pools, runtime-state defaults | Complete | `formats/data-ovl.md`; combat metadata now identifies the shared fixed 48-row, eight-byte combat-class table and adjacent per-class flag traits without publishing raw resident offsets or records. Magic metadata separates dense 48-entry spell token/recipe/scene-mask tables from the sparse long-incantation display phrase table, and virtue metadata includes the Blackthorn challenge prompt/response table ownership at semantic level. |
| `CASTLE.DAT`, `KEEP.DAT`, `TOWNE.DAT`, `DWELLING.DAT` | Location/interior tile grids | Complete | `formats/location-dat.md`; four-file scene partition, 16,384-byte file size, the flat sixteen-page array in each class file, the per-scene base floor-page table (the 2,048-byte two-page block is an authoring artifact with no runtime meaning, and the page is never derived as twice the sub-map index), signed floor-page loading, row-major 32-by-32 tile grids, marker harvest, cosmetic terrain variation, dawn/dusk substitution, and NPC floor-link markers are covered. Reachable-floor/content cataloguing, secret-room tile inventory, and visual/helper parity are delegated to catalog/runtime/presentation work; they no longer keep the base tile-grid file layout partial. |
| `MISCMAPS.DAT` | Cutscene maps plus Return-to-View map strips and command stream | Complete | Covered inside `formats/location-dat.md`; cutscene-map sectioning, Return-to-View map strips, section C, stream length, command/argument table, loop rule, actor/map side effects, exact EGA/Tandy cell-effect rasters, fixed rectangle sequence, preview-tick/input cadence, and abort state are specified. The formerly alleged short fixed wait is a presentation-only speaker effect. |
| `DUNGEON.DAT` | Dungeon level grid encoding | Complete | `formats/dungeon-dat.md`; fixed 4,096-byte eight-dungeon layout, record/level/cell ordering, packed class/variant byte preservation, scene/name binding, room-trigger arena selection, and static-file versus saved working-copy ownership are covered. Remaining lower-nibble naming belongs to runtime consumers, not the file layout. |
| `BRIT.CBT`, `DUNGEON.CBT` | Combat arena terrain and metadata | Complete | `formats/cbt.md`; fixed 352-byte arena records, eleven-by-eleven terrain grid, 32-byte row stride, stock record counts, complete traced metadata-reader census, unread-byte preservation, outdoor party-coordinate and monster-coordinate slices, dungeon party/source rows, ordinary/random-special/special conversion, every shipped special-source identity and state write, and Doom slot fifteen's terminal marker are covered. Arena edges are proved geometric rather than metadata-driven. `systems/combat.md` and `systems/dungeon-mode.md` own the fifteen-swap terrain placement-slot branch (**corrected 2026-08-31** from "dormant": it has two callers, and the surface camp-ambush route reaches it only with the shuffle bit set - `systems/combat.md` Section 5), the two live dungeon wandering-monster callers, the distinct sixteen-swap synthesis algorithm, exact PRNG order, presentation boundary, per-actor edge departures, and restoration. No traced dungeon chest path selects a separate arena record. |

### NPCs, Schedules, Dialogue

| File(s) | Contents | Status | Spec doc |
|---------|----------|--------|----------|
| `CASTLE.NPC`, `KEEP.NPC`, `TOWNE.NPC`, `DWELLING.NPC` | NPC roster/schedule records | Complete | `formats/npc.md`, `systems/npc-schedules.md`, `catalogs/npc-roster.md`; file size, sub-map stride, schedule record stride, slot-zero sentinel, one-based scene-to-file/sub-map selection, waypoint selection rule, AI-byte behaviour values, type-byte occupancy use, and dialog-index binding are covered. Remaining catalog naming and role-label interpretation belongs to `catalogs/npc-roster.md`, not to the `.NPC` file layout. |
| `CASTLE.TLK`, `KEEP.TLK`, `TOWNE.TLK`, `DWELLING.TLK` | Keyword-driven dialogue blobs | Complete | `formats/tlk.md`, `systems/conversation.md`, `catalogs/quest-graph.md`; per-class file selection, header layout (a two-byte count followed by that many id/offset entries; no sentinel row), fixed-window blob loading, obfuscation, mandatory entry order, keyword-pair grammar, reserved-keyword split, byte-runner/control semantics, dictionary substitution, and quest-graph boundary are covered. Remaining catalog reachability checks belong to quest-graph validation, not the `.TLK` file grammar or conversation interpreter contract. |
| `BRITISH.PTH` | Path stroke/movement stream | Complete | `formats/pth.md` |

### Text And Lookup Tables

| File | Contents | Status | Spec doc |
|------|----------|--------|----------|
| `LOOK2.DAT` | Surface/town look strings | Complete | `formats/look2-dat.md`, `catalogs/tile-catalog.md`, `systems/view.md`; 512-entry offset table whose entry *n* describes atlas tile *n*, lower terrain-description domain, upper object-description domain, the **two** distinct sentinel strings (`*` on twenty-three terrain rows, `x` on sixteen object rows) and the rule that a sentinel row implies nothing about drawability, shared string pool, direct domain lookup, plain-ASCII strings, command-handler suffix boundary, and validation rules are covered. |
| `SIGNS.DAT` | Sign/placard text | Complete | `formats/signs-dat.md`; scene directory, coordinate-matched records, formatter stream controls, macro substitution, pause token, high-bit presentation toggle, and wanted-poster separation are covered. |
| `KARMA.DAT` | Karma verdict text | Complete | `formats/karma-dat.md`, `systems/karma.md`; six-record verdict text layout, non-numeric nature, Blackthorn rescue/refuge five-band verdict selector, Lord British-in-disguise camp-event selector including sixth-record reachability, and the absence of traced shrine/endgame `KARMA.DAT` consumption are covered. |
| `STORY.DAT` | Intro story text | Complete | `formats/story-dat.md`, `systems/intro.md`; twenty sequential text records, proportional-font markup, the non-consuming inline doorway step, and caller-owned art/wait/transition boundaries are covered. |
| `QUESTION.DAT` | Character creation questionnaire | Complete | `formats/question-dat.md`, `systems/chargen.md`; thirty NUL-terminated records, plain-ASCII shipped content, brace/underscore proportional markup, pair-to-record ordinal mapping, and caller-owned pacing/stat effects are covered. |
| `ENDMSG.DAT` | Endgame message table | Complete | `formats/endmsg-dat.md`, `systems/endgame.md`; eleven NUL-terminated dialogue records, two visible confirmations, victory/refusal record ownership, and caller-owned branch/timing boundaries are covered. |
| `END.DAT` | Fixed endgame final-presentation narrative text | Complete | `formats/end-dat.md`, `systems/endgame.md`; proportional-text markers, six fixed caller-selected seek windows, plain-data loader boundary, traced final-presentation consumer, scratch-buffer reuse after `ENDMSG.DAT`, semantic grouping into return-home and Blackthorn judgment/gate windows, and separation from Lord British dialogue records and resident certificate text are covered. Any remaining visual page/panel parity belongs to the endgame presentation system, not this file format. |
| `MISCMSG.DAT` | Miscellaneous message table | Complete | `formats/miscmsg-dat.md`; sequential record model, forty-seven-record shipped count, Blackthorn/shrine/urn-Codex record-family ownership, ordinary text versus Codex tile-glyph presentation, and caller-owned state/branch boundaries are covered. |
| `SHOPPE.DAT` | Shop text, menu, bark, item-description, rumour, and inn-message records; stock and pricing live in resident tables | Complete | `formats/shoppe-dat.md`, `systems/shops.md` |

### Graphics, Fonts, And Bitmaps

| File(s) | Contents | Status | Spec doc |
|---------|----------|--------|----------|
| `TILES.16`, `TILES.4` | Main 512-tile atlas | Complete | `formats/lzw.md`, `formats/tiles.md`, `catalogs/tile-catalog.md`; shared LZW envelope, flat 512-tile atlas layout, EGA/CGA pixel encodings, tile stride, the terrain-half / actor-half split of the atlas index space and its numeral collisions with the monster sprite runs, the driver's mask-asset roster and the withdrawal of the over-general "driver-private, never drawn" set (`RETRACTIONS.md` R314), and the separate resident miniature tile-glyph boundary are covered. Remaining per-id visual attribution and runtime trace cross-checking belong to the tile catalog, not the atlas file layout. |
| `ULTIMA.*`, `ITEMS.*`, `TEXT.*`, `CREATE.*`, `DNG1.*`, `DNG2.*`, `DNG3.*`, `MON0.*`-`MON7.*`, `STARTSC.*`, `ENDSC.*`, `END1.*`, `END2.*`, `STORY1.*`-`STORY6.*` | Paired `.16`/`.4` graphics archive family | Complete | `formats/lzw.md`, `formats/tiles.md`; shared LZW envelope, all three post-decompression container layouts, EGA/CGA pixel encodings, directory/image headers, sprite mask polarity, body-size checks, and file-family roster are covered. The `ITEMS` five-family dungeon-object mapping and each `MONn` two-pose/three-depth monster mapping are now closed in `formats/tiles.md` and `systems/dungeon-mode.md`; presentation-specific runtime selection remains renderer ownership rather than an archive gap. |
| `IBM.CH`, `RUNES.CH` | 8x8 fixed-cell fonts | Complete | `formats/font-ch.md` |
| `IBM.HCS`, `RUNES.HCS` | 16x12 fixed-cell fonts | Complete | `formats/font-hcs.md` |
| `PROPORT.PCS` | Driver-compressed proportional font resource | Complete | `formats/font-pcs.md`; sparse-strip resource envelope, driver ownership, paragraph-renderer use, and the separation between resident width-table advances and file-backed glyph artwork are covered. The old width-table-plus-glyph-blob extraction hypothesis is superseded. |
| `TITLE.BIT`, `BRITISH.BIT` | Driver-compressed title/portrait bitmaps | Complete | `formats/bit.md`, `systems/display-driver-abi.md`; sparse strip resource format, driver dispatch ownership, title/menu idle ticking, and the story step-1 rectangle-transition handoff are covered. Exact resident helper wipe timing for slide/sub-screen transitions belongs to the display/intro renderer specs, not the `.BIT` file format. |
| `WD.BIT` | Driver-compressed Warriors of Destiny title lettering bitmap | Complete | `formats/bit.md`, `systems/display-driver-abi.md` |

### Save And Object State

| File(s) | Contents | Status | Spec doc |
|---------|----------|--------|----------|
| `SAVED.GAM`, `INIT.GAM`, `BRIT.GAM` | Full save/initial party and world state | Complete | `formats/saved-gam.md`, `systems/save-load.md`, `systems/chargen.md`, `systems/u4-transfer.md`; fixed 4,192-byte flat image, questionnaire seed, transfer seed, roster, inventory, spell-charge stock indexing by the canonical 48-row spell id, clock/save-neighbourhood offsets, location, save-backed moral-standing selector byte, active map/dungeon working buffer, mixed world/quest-state band boundary, active-object regions, timing/status glyph byte, transport/action marker byte, combat action-result scratch and its non-resumable lifecycle, room-clear bitmap, inn-registry marker/month-counter behavior, queued shipwright-delivery coordinates and packed class/payload byte, and companion `.OOL` relationship are covered at file-layout and seed-producer depth. Unnamed opaque bytes in mixed runtime bands and transport/action marker values outside known ranges must be preserved and remain runtime ownership/catalog work rather than save-image layout blockers. |
| `SAVED.OOL`, `INIT.OOL`, `BRIT.OOL`, `UNDER.OOL` | Persistent object/vehicle/NPC overlay layer | Complete | `formats/ool.md`, `systems/active-objects.md`, `systems/save-load.md`; fixed record/table sizes, surface/underworld split, load-time mirror writes, save-time staging, unconditional save-time mirror refresh, entry-mode-gated second `UNDER.OOL` flush, seed roles, active-object record boundaries, fresh-save no-normalization behavior, traced OUTSUBS per-plane mirror filename selection for town-entry and falls-transition setup, ship hull/skiff auxiliary bytes, broadside byte-5 depletion, and byte-6 packed frame-delay / animation-script-step semantics (no facing; R340) are covered at file-layout and lifecycle depth. Uncommon family-specific meanings for bytes `+5` and `+7`, any future mirror-reader callers outside the traced OUTSUBS transition paths, and underworld object population sources are runtime/catalog work rather than `.OOL` layout or save/load mirror blockers. |

## 3. System Specs

| System | Status | Spec doc |
|--------|--------|----------|
| Launcher/startup | Complete | `systems/launcher.md`, `systems/boot.md` |
| DOS runtime and overlay model | Complete | `systems/runtime.md`, `systems/timing.md`; `systems/timing.md` Section 8.2 owns the overworld under-sail auto-advance cadence (**issue #189, 2026-09-03**: the two-tick figure is a maximum, not a cost; each wait pass is a fully consumed turn; the same-direction key is swallowed. Reversals R371, R372.) |
| Gameplay PRNG | Complete | `systems/prng.md`; the idle-path consumer inventory is corrected (**issue #182**, R331/R332): **three** consumers run per idle pass, not two - the active-object animator draws first, then the wind check, then the composite - and the wind check's retry loop takes **single** draws, so its per-invocation count is one, two, three and upward rather than "pairs". The composite's own cost is one draw per composited actor **that lands on a selecting terrain row** and zero for every other, which is frequently zero for a whole pass. `systems/combat.md` Section 5.3 carried the same withdrawn pairs wording and now carries the correction. |
| Intro/menu/story flow | Complete | `systems/intro.md` |
| Character creation and U4 transfer entry | Complete | `systems/chargen.md`, `systems/u4-transfer.md`; questionnaire stat finalization, fixed Avatar class, seed-preserved HP/experience/level fields, starting counters/reagents/clock/location, seed-preserved item/spell/quest-stock bands, factory-seed readied equipment, abort-before-write behavior, questionnaire pair-to-record mapping, `PARTY.SAV` transfer source, transfer source validation ranges, transfer Avatarhood test over the eight predecessor virtue/karma standing words (all-zero is the Avatar success condition, never a rejection; the older "no-data gate" framing is withdrawn), transfer source field offsets re-derived from the two source reads, transfer slot-0 target, transfer class/gender/status mapping, primary-stat conversion, MP derivation, scaled XP, derived level, derived HP, fresh-save `.OOL` emission order, first-load no-normalization mirror behavior, transfer preview region layout, and transfer post-commit return through intro/menu redraw are covered at fresh-save producer and preview-region depth. Exhaustive transfer preview text-field cursor, attribute, and redraw-timing parity remains presentation work rather than a chargen or transfer-entry contract blocker. |
| Main loop and command dispatch | Complete | `systems/main-loop.md`, `systems/commands.md`; resident startup handoff, one-character display selector policy, scene-byte dispatch, playable mode-loop ownership, combat's non-outer-loop boundary, world-tick/redraw placement, per-turn cleanup placement, save/load resume, resident Q save routing, explicit exit prompt separation, and A-Z command routing are covered. A-Z routing now includes N-New Order as a whole-record active-party swap rather than a separate canonical companion-order table, H-Hole-up routes to the shared rest/camp contract, and Y-Yell's non-vehicle branches are split into Shadowlord-name and Word-of-Power context routes. Section 9 now carries the blocking-presentation summary and withdraws the claim that the world tick is reachable only from the idle wait (R319). |
| Movement and passability | Complete | `systems/movement.md`, with mode hooks in `systems/overworld.md`, `systems/town-mode.md`, `systems/dungeon-mode.md`, `systems/combat.md`, `systems/vehicles.md`, and `systems/npc-schedules.md`; shared direction routing, corrected world/town base terrain bitset shape, caller-query tile-class dispatcher boundary, named foot/horse/carpet/ship/skiff query families, exact accepted static tile ranges for those named movement queries, named non-vehicle active-object predicate families where the bestiary/encounter specs identify the sprite run, generic predicate treatment for unnamed non-vehicle query families, LOOK2-named lava/chair compatibility edge, the separate NPC pathfinding bitmap/ranges, vehicle layering, X-Xit nearby-support probing, dynamic occupancy, and commit/redraw rules are covered. Final art/catalog names remain catalog ownership, not a movement-system gap. |
| Input pipeline | Complete | `systems/input.md`; eight-code direction input vocabulary is separated from cardinal-only world/town/dungeon/combat movement consumers, and diagonal fallthrough behavior is covered. Shared party-member selector return families are covered, including selected active slot, cancel, and explicit-none results. The resident adjacent-tile command direction prompt is covered as a cardinal-only vector selector with Space/Pass cancellation. The shared spell direction prompt is also covered: world/combat target origin selection, cardinal target adjustment, Space/Pass no-direction result, and re-prompt on other keys. |
| Display driver/rendering contract | Complete | `systems/display-driver.md`, `systems/display-driver-abi.md`; v1 semantic rendering contract, corrected EGA rectangle fill, compressed-bitmap resource decode, front-buffer tile/glyph paths, EGA dispatch ABI, and visible page-zero/back-buffer copy boundary are covered at the EGA/v1 compatibility depth. Exact alternate-driver conversion remains historical hardware parity work outside the v1 rendering contract. |
| Text output/windowing | Complete | `systems/text-output.md`; fixed-cell resident windows, the typed-input erase/padding helper, cursor-advance gate ownership, and the separate FONT proportional paragraph renderer are covered. Window capacity is corrected (**issue #185, 2026-09-02**): both corner columns are inclusive and the emitter wraps only on stepping **past** the right column, so a row holds `right - left + 1` characters and the gameplay message window holds **sixteen**, not fifteen; the long-word case hard-breaks at the row edge rather than overflowing it, and the full-row line-feed suppression applies only within one source string. Reversals R344-R346 and R348. |
| Save/load | Complete | `systems/save-load.md`, `formats/saved-gam.md`, `formats/ool.md`; the load path's recompute-nothing contract and the attribution of every apparently load-written byte to the first mode dispatch after the load, the preserving entry mode that dispatch passes to a town-family scene, and Q-save's absence of any store into the save image alongside the `.OOL` pair's read-then-concatenate round trip (**issue #184**); byte-image save/load, `.OOL` companions, per-plane mirrors, empty-save guard (**issue #182**: the guard's exact predicate is now published in `systems/intro.md` step 7 as well - `SAVED.GAM` byte `0x0002` alone must be non-zero, evaluated only on the Journey Onward branch after the full 4192-byte read; an implementation that scans the whole name field or trims spaces disagrees on a name beginning with a zero byte), inn-registry round-trip behavior, queued shipwright-delivery persistence exclusively through `SAVED.GAM`, current map/dungeon working-buffer ownership, mixed world/quest-state band preservation, resident read/write primitive edge cases, write-side critical-error handler ownership, fresh-save `.OOL` no-normalization behavior, and single-slot overwrite semantics are covered at the four-file byte-image contract depth. Unnamed opaque bytes, unknown transport/action marker values, and optional modern backup/versioning policies belong to owning runtime specs or extension policy, not the original save/load contract. |
| Overworld/underworld | Complete | `systems/overworld.md`; outdoor scene entry, chunk loading, live-buffer substitutions, movement commit, special underfoot latch, whirlpool forced-underworld movement, active-object per-turn handling, encounter probe inputs, save/object-overlay ownership, mode hooks, traced active-object/encounter pendulum gates, saved-slot natural moongate live-tile refresh/waning, live terrain `0xDC` shimmer/entry handling with the midnight shrine/urn dispatch and saved-slot warp path, natural-shared mode-local scratch writer census (outdoor chunk-loader and town marker reuse plus combat phase reset); no per-frame moongate animator exists, all-disassembly and binary direct-write scans finding no additional static writer to the animator coordinate block, shipped-map scans finding no static natural-moongate tile cells in `BRIT.DAT`, the bounded plane-writer census showing no additional outdoor underworld-to-surface writer beyond traced fall/whirlpool/interior-exit cases, the corrected waterfall-tile-family falls trigger with its two forced southward steps and its brink-cell census of both shipped maps, the corrected fall-damage roll, and the exact falls/whirlpool result lines with their rendered line breaks, sound, disk handoff and non-waits, the fixed narrative gate branch as a post-action surface-coordinate check, and the distinction between live terrain byte `0xDC` as a moon-gate-family destination and active-object type byte `0xDC` as the first Dragon frame are covered, with encounter probe and random-spawn behavior complete at overworld-loop depth. (**issue #185, 2026-09-02**: Section 8.1's fifteen-column figure and its "narrowed exactly once" claim are withdrawn - R347, R348; the rendered rows are unchanged.) (**issue #189, 2026-09-03**: Section 5 step 3's "does not read the keyboard at all" is withdrawn - the under-sail auto-advance loop polls one key per pass and swallows a key matching the cached heading; Section 6's pendulum-gate paragraph is re-attributed from the animator to the outdoor per-turn block. Reversals R372, R374.) Tile naming, opaque timing/transport values, and chunk-substitution labels are catalog or QA work. |
| Town/interior mode | Complete | `systems/town-mode.md`; entry, map load, marker harvest, dawn/dusk substitution, cosmetic variation, movement/floor/exit transitions including the town-family exit thresholds and facing-sensitive `0xC4..0xC7` town stairs (tile `0x59` is the telescope Look trigger, not an exit threshold), command hooks, resident Shadowlord installation and its row-four coordinate bypass, alarm/arrest handling including the three bounded entry routes, the sole explicit-T producer of the arrest-cleanup command result, and the surrender-to-Yew-jail setup, the live NPC-conflict handoff into ordinary terrain combat and the post-fight town reload, NPC schedule integration, active-object ownership, free-roaming object movement, Stonegate entry presentation producers, entry-mode preservation behavior and traced caller census, authored secret-room mechanism delegation, and save/load entry reconstruction are covered at behavioral-contract depth. The earlier phantom-player NPC attach, no-arena-combat hostile-NPC boundary, and generic "conversation happened" reading of the special Talk result are withdrawn. Stonegate presentation-asset parity, authored secret-location inventory, and rare nested script-return parity remain presentation/data/empirical work rather than town-loop blockers. |
| Dungeon mode | Complete | `systems/dungeon-mode.md`; dungeon scene/record binding, cell layout, live cell-byte visibility boundary including class-sensitive `0x08` handling, sparse point-plotting first-person renderer, binary renderer light gate, command parser, movement and turning, active-monster collision, L-Look/V-View including the L-Look relative-focus chooser, Space/Pass abort, temporary V-View scratch visited grid, and exact minimap class-to-glyph/flood-return table, K-Klimb including ordinary ladder caps and exact `0x60` surface-reset pit path, room combat triggers including Doom level seven room id fifteen as the terminal final-room route, saved room-clear bitmap plus reload-time demotion, post-action monster engagement, post-action pit/bomb traps, the exact post-action consequence lines (room entry, sleep/poison/fire fields, electric contact, the three-line pit group, the two bomb lines), movement verbs and refusals, Klimb prompt family, darkness refusals, Search preamble and outcomes, fountain drink flow, chest command lines and the four chest-trap words, the five blocking prompts and the seven sound families with their stated scopes, Search's light-gated high-nibble feature classification, chest trap-tier narration, pit-family secret/bomb handling, flavour/wall visit-local rewrites, Get's open-chest visit-local consumption, the negative baseline rule for wind/breeze torch-extinguish contact, the dungeon-flavour presentation-only boundary, pit-chain off-bottom state handoff, and the stock-data boundary that no shipped fall-trap placement can produce the off-bottom path are covered. Low-nibble naming outside published gameplay subtypes and V-View glyph/pixel confirmation are catalog or presentation QA, not dungeon-loop behavior gaps. Dungeon room arena indexing is covered; no traced dungeon chest path currently loads `DUNGEON.CBT`. |
| Combat | Complete | `systems/combat.md`; combat-entry presentation (Section 4.1, **issue #185, 2026-09-02**: the group-name banner's emission order, its centring-as-cursor-move rule, its count-independence, and the conflict banner's exact literal, `0x2A` flank glyph, edge-to-edge placement and suppressed trailing line feed), core loop, framer, combat descriptor row shape, complete dispatcher-level command map, delegated overlay targets for most combat commands, rest/camp alternate setup target and skip-combat gate, combat U-Use routing into the item-use handler (not a label-only abort), combat Yell's CMDS no-effect scene fallthrough, combat Q-Quit refusal (no save route and no fight exit from inside combat), X-it-vs-edge-flee distinction, out-of-arena leave/escape helper including ship-style refusal and constrained same-direction exits, target selection including phase/invisibility filters, Doom and Shadow Lord suppression-filter exceptions, no-target centre fallback including the direct flee-flag/critical-HP marker writer, ordinary AI action selection (**corrected, issue #185**: the driver calls the shared primitives directly and enters no command parser - R353), 48-row class-stat layout, shared weapon/spell damage-row split, item-id keyed weapon range/effect rows, range-gated melee versus ranged/projectile routing, shared to-hit formula, class-flag monster special hook with decoded row assignments and opaque component-bit policy, teleport-capable movement and ordinary movement fallback helpers, combat C-Cast interference-source writer/revalidation/clear/persistence lifecycle, Amulet/Turning's target-side ranged/effect scatter branch including the Mage non-monster row boundary, ranged/effect branch selection and magic-immune gating, dual-use monster range/damage-selector and payload side-table semantics with source-free row values, spell field contact, monster poison/status attack gating, special combat absorption marker handoff, the combat-only cursor-blink and marker tail of the shared viewport rasterizer with exact EGA/Tandy geometry, colours, overwrite order, clipping boundary and repaint-based erasure (**corrected 2026-08-23**: previously listed as "post-round terrain/effect maintenance"; withdrawn - see `systems/combat.md`), damage/death including Gargoyle/Gazer special deaths and boss-style vanish-on-death assignments plus the vanish branch's shared action-result lifecycle, exact 256-pixel terrain reveal, partial slot release, and mutating party control/faint tail, ordinary attack/spell experience credit, Cause Fear/fear-panic critical-HP flee setup, low-HP wound-score morale as the flee-bit writer, summon/tame-style spell-side actor repurpose using descriptor bit `0x01` rather than the flee bit, shared P-Push movable-tile behaviour including combat actor-anchor invocation, active-effect consumers, framer non-merge of temporary drop markers, caller-side original trigger-slot reconciliation, absence of combat-exit gold/karma/victory-bonus writes, absence of ammunition/thrown/glass item mutation across the traced combat attack stack and separate traced equipment consumers, the absence of a traced post-fight SJOG loot sweep, the full ordinary to-hit/damage contract with published hit percentages and a worked Bat-versus-starting-Avatar example (issue #183), and the complete attack-outcome narration census of Section 11.1 with its order, newline and cue contract for both sides (**issue #185, 2026-09-03**; reversals R352-R355 withdraw the zero-damage "miss" narration, the fabricated monster attack announcement and its synthesised-keystroke framing, the AI-driven reading of a controlled monster, and the unqualified silent-monster-miss claim in `systems/audio.md`) are covered. |
| Encounters | Complete | `systems/encounters.md`, `systems/rest-and-camp.md`, `systems/traps.md`; the entry banner's `PIRATES` fallback is now published as a `masked < 0x40` range guard rather than a `0x2C` ship test, with the reachable-sprite set on two ungated entry paths left open (**issue #185, 2026-09-02**, reversal R350); random/scripted/dungeon trigger families, eligible-turn pendulum gating for the overworld random probe, the tile/Z/hour random-encounter threshold formula, random-spawn coordinate retry loop, corrected terrain bucket selector, ordered weighted random-spawn bucket memberships, named active-object payload families including the whirlpool, parched-desert Sand Trap (corrected 2026-08-23 from a "sea-serpent adjacency" mislabel) and Rot Worm special cases, outdoor arena selection ranges, ambush/camp reveal-slot lifecycle and `DATA.OVL` record shape, H-Hole-Up rest-loop duration/watch prompt, Good-status watcher validation, 1-in-64 interruption predicate, eight-row monster chooser, caller-side ambush-message/status restoration ordering, selected-row handoff, and CMDS alternate setup target, shared party trap-effect resolver, dungeon room arena indexing, double-encounter flag read/save/28-day-month-clear/count-reroll behavior plus no static setter found in current sweeps, the absence of a traced dungeon chest arena caller, ordinary combat-local experience credit, the combat-framer non-merge boundary, the terrain-helper placement shuffle and its two callers (**corrected 2026-08-31** from "dormant/unwired"), caller-side original trigger-slot reconciliation, absence of combat-exit gold/karma/victory-bonus writes, and the absence of a traced post-fight SJOG loot handoff are covered. Source-free reauthored ambush reveal row values are needed only for a non-original-data publication target; ambiguous payload-frame verification belongs to tile/catalog and presentation QA, not the encounter behavior contract. (**issue #189, 2026-09-03**: Section 2.1 now states that the encounter probe is **memoryless** - the "phase/countdown nibble" does not exist - and that the alternation state is two one-bit toggles owned by the outdoor per-turn block, each flipped by the gate it controls. Reversal R370.) |
| Blackthorn capture/rescue | Complete | `systems/blackthorn.md`, with data references in `formats/location-dat.md`, `formats/miscmsg-dat.md`, and `formats/karma-dat.md`; capture audience setup, town-side captive-scene arrest-handler entry predicate, the shrine-selection scan over the shrine ruin flags (corrected 2026-08-22 from "already-jailed skips"), withdrawal of the alleged caller-provided death-route marker (the tested byte is the sound setting and changes only one presentation pause), transient conversation-signal boundary, the mantra interrogation, the full eight-shrine virtue/mantra mapping, response matching, the punishment execution and its durable roster effect, exact two-guard versus seated-Blackthorn slot roles, cutscene script VM state, actor-indexed movement semantics, five traced script beats, exact quiet-pause and redraw semantics (including withdrawal of the alleged output-byte and screen-clear commands), terrain/actor tile identities and cell positions, direct 256-pixel cell reveals, rescue flash/dissolve boundaries and deterministic observable vectors, rescue `KARMA.DAT` five-band verdict selector, durable shrine-ruin / roster-removal / moral-standing state, zero-only Food safety grant, absence of captive-counter/context storage, temporary cinematic actor boundaries, scene-key handoffs, and exact town/overworld/dungeon entry predicates are covered through pixel-level cinematic contract depth. |
| Conversation | Complete | `systems/conversation.md`, `systems/quest-flags.md`, `formats/tlk.md`; Talk entry, fixed-window `.TLK` loading and cap edge, opening description/greeting preamble, fixed reserved-keyword scan, ordinary per-NPC keyword scan, NAME/JOB-WORK/BYE-THANK aliases, profanity/default rebuke plus bounded pause/timing loop and negative direct-karma-write boundary, three-digit gold-payment debit plus toll-milestone scalar selector path with no direct per-virtue standing write, JOIN/ASK-WHO control-byte boundary, protected-run print-mask behavior, branch flags including out-of-range zero-mask behavior, gold/action dispatch, label-record/scoped-prompt byte contract including inner empty-input behavior, reserved-word suppression, and label-response stop-versus-NUL returns, common-word dictionary, common-counter/Grapple/carpet/skull-key and Spyglass/Sextant/Black Badge action-letter effects, final cleanup sentinel predicate, stolen-action warning glissando, random gold fallback, and shared sentinel producer/cross-use with the shop surcharge gate are covered. |
| NPC schedules/pathfinding | Complete | `systems/npc-schedules.md`, `catalogs/npc-roster.md`; roster loading including exact one-based scene-byte to file/sub-map mapping and temporary loader-side zero-based conversion, hour-to-waypoint selection, runtime state, cached-waypoint transition memory, per-turn walking, boundary state classification including the parked off-floor state, pathfinding workspace/BFS, hardcoded destination-relative dynamic-obstacle radius, floor-link markers, sprite placement, hidden-mask visual suppression boundary distinct from the town activation/death mask, shipped hidden-mask scene/slot catalogue plus anonymous-slot role-label boundary, persistence omission, the published stuck-counter contract (one increment event, the short forced-replan threshold, and the high band), stairway route-selection ownership with the visible tile-facing contract delegated to town/location specs, out-of-town actor ownership boundary, and town/rest invocation cadence are covered. The long-stall high band is observable gameplay - five consecutive still turns, entered by assignment after a failed replan, with the entering turn itself not still - not presentation parity (R369). The per-turn wander gate is now published (Section 9.1): one attempt per NPC per turn behind a one-in-two coin, a single slightly-east-biased cardinal draw, a constant waypoint cap of three for AI `1` and `4` alike (R317 withdraws the earlier shrinking-range reading), no retry after a rejection, and the reconciliation of that rule with the reported eight-steps-in-twenty-four-turns measurement. Also published: the schedule-boundary diversion and its two escape paths, and the town turn loop's three effect gates with their town-specific order. (**issue #189, 2026-09-03**: Section 9.1 now publishes the replan draw's exact form - a uniform three-value range draw accepted on the middle value, not a mask - together with the counter's three bands, the five-turn high band and the fact that the turn entering it is not still; the stuck counter is reserved for the queued-route step refusal alone; Section 12 answers the animator-cadence question as descriptive with two named residues, and its animator paragraph is corrected from "draws the sprites, each render frame" to "selects frames, once per world tick". Reversals R367, R368, R369, R373.) |
| Doors and Z transitions | Complete | `systems/doors-and-z-transitions.md`; ordinary door Open/Jimmy behavior including the NPC pickpocket thankful-state and capped moral-standing `+2` path, magic ordinary-door opening, dungeon Word-of-Power door transmutation, outdoor Grapple Klimb, town-floor Klimb, facing-sensitive `0xC4..0xC7` town stairs, the thirty-two-by-thirty-two town-family grid-boundary exit prompt (tile `0x59` is the telescope Look trigger, not an exit threshold), dungeon level changes, dungeon pit-chain off-bottom state handoff plus stock-data non-reachability, vehicle X-Xit boundaries including current-cell parking and nearby-support probing, combat enter/exit state preservation, confirmed surface-to-underworld chasm fall (waterfall-family trigger, not a coordinate), whirlpool forced-underworld movement, scene-`0x19` interior exit plane selection with its exact prompt/answer transcripts, the exact dungeon-exit lines and their wrap-derived line breaks, dungeon surface-reset handling, bounded current census showing no separate outdoor underworld-to-surface writer, and visit-local handling for door, magic-lock, secret-door, cannon-destroyed-door, dungeon Search tile rewrites, and P-Push top-down live-buffer non-durability plus `0x44`/`0x45` cobble-stamp semantics are covered. Residual per-tile labels and town secret-door object encoding belong to tile/catalog or location/object data-format docs rather than the transition contract. (**issue #185, 2026-09-02**: Section 12.1's width figure and its "restarts the word at column zero" mechanism are withdrawn - R347, R349; the rendered rows are unchanged.) |
| Containers and pickups | Complete | `systems/containers.md`, `systems/hidden-treasures.md`, `systems/traps.md`; command flow, pickup object matching, fixed hidden-treasure scan shape, complete 113-record location/pickup table, one-shot tracking, pickup staging, consumed-object persistence by object-slot clear/live-tile rewrite/found bitmap/Moonstone invalidation, exact rare-reagent Search harvest coordinates/cooldowns/quantity rolls, Moonstone recovery, Search slot-treasure and live-tile fallback classification, broad chest/container behaviour, top-level inventory-add class mapping including potion/scroll/key/odd-key/shard/equipment subtype handling and counter caps, per-object Search trap-tier threshold narration including false positives and missed traps, surface/town chest primary and secondary content pools, table-food reach rules, borrowed-furniture tile mutation with no traced moral-standing debit, exact dungeon chest seven-row reward table including the depth-zero gold-row helper edge, and shared party trap-effect resolver with effect distribution are covered. No container-owned consumed-object persistence, ordinary counter-cap, or inventory subtype gap remains; residual item-use effects are owned by the item and magic specs. |
| Vehicles and ship fire | Complete | `systems/vehicles.md`; boarding/dismounting, exact B-Board object-byte to transport-state transitions for horse, carpet, ship, and skiff, the negative boundary that balloon art is not a traced live transport path, X-Xit current-cell parking, nearby landing-support probing, carried-craft transfer branches including ship skiff launch and stowed-carpet redeploy, ship-fire routing, Y-Yell sail toggling, active-object integration, transport-marker ranges and facing/sail encoding for foot, horse, carpet, ship, and skiff, ordinary vehicle terrain-query families, mounted-horse one-cell movement with the standard overworld turn cost and horse-specific passability, ship boarding precondition boundary including foot, skiff, and accepted carpet markers `0x14`/`0x15`, traced outdoor alternate-turn actor/encounter pendulum pairs, horse-trader Talk purchase placement, ship-broker Talk purchase trigger, shipwright active-object placement handoff, shipwright Frigate/Skiff payload semantics, ship hull/skiff auxiliary-byte semantics, overworld broadside byte-5 depletion and slot-clear behavior, town-family cannon active-object hit moral-standing selector debit, and save/transport state are covered. Balloon art does not define a command path for the analyzed baseline; residual vehicle art-frame naming and transport/action values outside the known ranges are catalog or opaque-state work, not command-transition gaps. |
| Rest and camp | Complete | `systems/rest-and-camp.md`, with hooks in `systems/time.md`, `systems/encounters.md`, `systems/town-mode.md`, and `systems/overworld.md`; H-Hole-up scene routing, terrain/bed gates, hours prompt boundary including town bed rest's subtract-23 target-hour wrap edge, town-hours scheduler burst and ten-minute cleanup loop, overworld/dungeon duration input, optional watch prompt, Good-status watcher validation, dungeon rest/camp alternate handoff, dangerous-rest 1-in-64 interruption predicate, party recovery, rest-owned status transitions, sleep-ambush ambush-message/status restoration ordering, selected-row handoff, CMDS alternate setup target, and the surface camp-ambush route's use of the terrain-helper placement shuffle (**corrected 2026-08-31** from "non-use of the dormant ... shuffle"), Lord British's outdoor camp-event level-up including its completed-long-camp and duration requirements, live dungeon-context suppression, dormant reserved suppression input, pre-draw context ordering, single 25% eligible trigger roll, experience-derived level recomputation, HP reset, uniform capped stat reward, and class-specific MP refresh, and delegation of food/provision consumption to the shared hourly time cadence are covered at gameplay-contract depth. Low-level rest/camp string-window, audio/delay, and refresh-helper parity remains presentation work rather than a rest-system setup question. |
| Active objects | Complete | `systems/active-objects.md`; the town-family entry-mode argument and everything the preserving mode suppresses (**issue #184**, R341), record byte `+6` as packed frame delay and animation-script step carrying no facing (R340), and the scene-scoped round-trip qualifier forced by the wholesale per-plane object-list reload on every overworld return (R342); shared table shape, slot allocation/freeing including the protected `0xB5` monster-variant actor class, player slot, NPC linkage, player-as-NPC mirror separation from live collision blocking, combat backup/restore, renderer/visibility hooks including the direct companion-band compositor branches and default helper terrain remaps, persistence, ship hull/skiff auxiliary bytes, combat descriptor separation, field-marker lifetime, projectile/impact visuals and natural moongates as ordinary live terrain rather than animated active-object lifecycles, and overworld per-turn hostile-slot movement including randomized X/Y directed-step priority, random-wander fallback, validation gates through the shared tile-class dispatcher and reverse active-object lookup, destination-tile movement chance gates, per-pass last-vacated-cell guard, water-creature facing rewrite, special mover chance-gate bypass classes including the active-object type-byte `0xDC` Dragon-frame boundary, committed-step clearing on live terrain byte `0xDC`, the `0xFC` proximity mask, and redraw dirtying are covered. No core active-object table lifecycle gap remains for known traced users; future uncommon non-projectile dynamic-object callers, if found, belong to their owning systems. The animator's write set is now published as facing and displayed frame only (R316): it moves no actor, the outdoor walker has no per-turn movement roll of its own, and a town's loose horse-family objects belong to a separate town-mode object walker with its own one-in-two per-slot coin. (**issue #189, 2026-09-03**: Section 8 now publishes the animator's per-pass early-outs in order, its shared-generator advance accounting, and the reader position for both bytes it writes - the facing/phase negative is **probable** within a stated census scope. Section 13 records the open dungeon/combat cadence question.) |
| Visibility | Complete | `systems/visibility.md`; the actor **variant selector is corrected** (R302): its short-circuit input is the global timed-magic-effect code and fires under Negate Time, not a Tinker class letter. **The selector's reachability is now re-scoped (issue #182, R329)**: only five rows of the Section 8 table select at all, the two chair rows require a *laden-table* id on the neighbouring row with an accepted set that **differs per facing**, roughly half the shipped chairs fall through to one fixed tile with **no draw**, and the bed stamps a single fixed tile and is not a variant - so an engine that draws once per *composited* actor desynchronises the shared stream. Within that narrowed arm the draw remains normatively once per composite pass with no cache anywhere (Section 8.1), and only the two chair rows are reachable by the party at all - stocks, manacles and mirrors are blocked terrain and NPC-only. That merged furniture sprites **visibly** re-roll while the player stands still stays at **probable**, not established (Section 8.3), but for a changed reason: not merely that nobody watched it, but that the arm is narrow enough that an arbitrary seat is as likely as not a fall-through, and two of the five tile sets are visually marginal (one differs by a handful of pixels per frame; the trapped-soul set is effectively two images, not four). A capture settles it only if it names the cell and the neighbouring-row terrain. Combat reachability is now **closed narrowly**: outdoor arenas hold none of the furniture terrains, dungeon arenas hold four manacle cells, one mirror, no stocks at all, and **exactly one** selecting chair game-wide, with no `0x90` seat able to select in combat. The `0x9E` mirror row is **dead** in the shipped game over a stated file list. The `0x5C` compositor arm is re-identified (R330) as one ordinary NPC sprite family, not a vehicle/avatar branch. The Negate Time producer census is corrected (R333): **two** producers, the spell at ten turns and the scroll at twenty. Section 13's previously unspecifiable renderer branch is identified as the moon-gate cell's rise-and-sink blit, gated on the shared gate-presence counter - and it needs no new contract, because `systems/overworld.md` Section 9.1 already publishes the counter-to-artwork mapping and the counter's lifecycle; Sections 9 and 13 of the visibility document now defer to it. It is not a moon-*phase* term. The composite and raster tail is unconditional and is **not** gated on the visibility dirty flag — that flag only selects among the three grid producers, whose restoration behaviour differs and is now published per producer. Also covered: viewport buffer, fog post-pass, the ambient lighting value as an inclusive squared-distance threshold rather than a radius, including the negative-light full-fill branch, actor compositing including default active-object companion-band remaps, queue-based centre-out visibility carve shape, propagation-blocker set, adjacent-only special-case propagation, read-only renderer/effect consumption of marker bytes, compositor-owned zero-cell lifecycle, the 32x32 local-light mask refresh, and moongate-mask writer ordering are covered. Display-driver palette/art parity for marker bytes, external-reader synchronization policy, and dungeon visual exactness belong to display/tooling or dungeon-mode work rather than the 2D visibility-grid contract. The negative-light full-fill branch is now published as **live** behaviour rather than compatibility scaffolding (R327): the spell/potion visibility sweep drives it deliberately, which is how the White potion and the X-Ray spell reveal the whole window through walls. **Issue #188 closes the three residual compositor questions in new Section 8.5**: the grid-versus-map read asymmetry is deliberate and prescriptive (map at the absolute coordinate for the default helper, visibility grid at the projected viewport cell for the `0x5C` branch, both taken for one actor in one frame, both reachable from shipped roster content); combat enters the **same single compositor** through the shared world tick and differs only by five named skips, withdrawing Section 11's separate-combat-compositor model (**R365**, which also corrects the same model in `systems/combat.md`, `formats/cbt.md` and `systems/active-objects.md`); the neighbour probe is **unbounded in an arena** (off-record residue, unreachable in shipped arenas, engines should answer *no match*) and **substituted on a location floor** to the southeast-corner cell `(31,31)`, taken first-hand by the compositor and second-hand by the town movement predicate. The out-of-range query is a substitution into the live map buffer, not a fixed sentinel tile (**R366**). |
| Time/calendar/moons | Complete | `systems/time.md`, `systems/moons.md`, `systems/overworld.md`; the twelve-hour byte's snapshot-mismatch write gate, its single audio consumer and two-in-eight decay, and the saved-hour snapshot's non-refresh on a mode-zero call (**issue #184**, R338), plus the moon-phase renderer's caching of the two glyph digits that moongate transit reads (R339) and its corrected multi-caller refresh cadence, which withdraws the below-surface erase-arm unreachability (R343); the **issue #190** closures on top of that — the erase arm resolved as live on four routes, the caller census published as a table with its scan scope, in-place overworld returns and in-place location floor changes both confirmed as refresh sites, the moongate warp's origin-and-destination guard, both command-handler callers identified as the two arms of `H`, the audience cutscene corrected to one effective repaint (R375), the ambient sub-tick residues named as zero and four with a program-start phase origin, and the unchecked out-of-range day-of-month lookup that withdraws the save-data-error instruction (R376); minute/hour/day/month advancement, major per-turn callers, timing-tag effects, calendar save offsets plus adjacent persistent state ownership, daylight endpoint/sentinel values, exact dawn/dusk gradient levels, torch/spell personal-light floors, hourly party-status and food/provision cadence, inn-owned month-counter billing, no-separate-active-consumer month-counter boundary, resolved hour-change sky/status row refresh, twelve-cell fixed-hour-marker/Trammel/Felucca status-strip rendering with exact visible-hour windows, overwrite order, independent day-indexed moon glyph tables, natural-shared mode-local scratch-input shape and writer census (no per-frame moongate animator), saved-slot live-terrain moongate refresh linkage, mode-local scratch reuse boundary, fixed ordained-bitmask overworld narrative gate branch including post-action ordering, live moongate entry ownership delegated to the overworld contract, bounded mode-zero recompute caller census for traced overworld/town, underfoot-light latch, combat-exit, dungeon-room, save-load paths, and the ordinary town arrest wait-to-morning twenty-minute cleanup loop are covered. Moongate entry is overworld-owned behavior, not an unresolved time/calendar/moon-display path. |
| Lighting | Complete | `systems/lighting.md`; daylight, original light-scale endpoints, exact dawn/dusk gradient levels, torch/spell personal-light floors, torches, light spells, saturating counter decay, the overworld `0xFF` underfoot zero-light override, and the ambient skip-recompute sentinel contract are covered. Current writer analysis found no normal gameplay scene that deliberately sets a high lighting sentinel, so preserve the sentinel as defensive compatibility behavior rather than modeling it as a named scene. |
| Weather/wind | Complete | `systems/weather.md`; the single-writer census and the finding that neither load nor scene entry rerolls wind (**issue #184**), the drift roll's per-idle-world-tick cadence and master-redraw gate, and its 1/17 Calm : 4/17 per-cardinal stationary distribution, with every wall-clock interval statement labelled inferred rather than measured; wind presentation states, source-direction compass convention, DATA.OVL label order, saved wind-byte mapping/preservation, out-of-range wind banner behavior, idle-redraw random wind selection, Wind Change prompt-to-state mapping including Space/Pass no-effect and calm handling, ship sail-state markers, hoisted-sail player-ship wind cadence including calm/stalled feedback and HMS Cape wait-pass timing, non-player water-creature/pirate active-object wind cadence, and the ruled-out MAINOUT wrapped-call pause helper are covered. Post-cadence step selection is specified under active-object movement rather than weather. (**issue #189, 2026-09-03**: new Section 5.1 publishes the sail-direction cache's complete lifecycle - one setter, four clears, marker-conditioned rather than command-conditioned - plus the crosswind fold and the wind-change reset of the sailing counter. No reversal; a new heading replaces the cache and the spec never said otherwise.) |
| Animation | Complete | `systems/animation.md`; **three** layers, not two: the per-slot active-object pass, the five-family frame-selector pass, and the driver-side tile-asset pass (Section 12) that rewrites the loaded tile artwork in place every animation step and is what animates open water, shoals, lava, the river/grass/water composites, every fire fixture; the banner and sail tiles are attributed to the same pass only at **probable**, not established (Section 12.5) - that stage was identified in the same re-derivation but was never given its own verification pass and none of it was observed at runtime. Also covered: the identity-initialised selector table and its renderer contract; `0xE8..0xEB` as an authored frame group no animator advances, with its cutscene consumers and one unreachable frame; `0xFA`/`0xFB` confirmed as two phases of one clock with a full placement census; the three callers of the animation step with their gates, the Negate Time freeze, the master-redraw-gate freeze and the Tandy-only first-pass sentinel; and per-family rate ratios stated as ratios rather than absolute rates. Cadence is now anchored to a black-box measurement contributed by the clean implementation side (54.913 ms per step, timer-driven, confirmed at two CPU speeds); `systems/timing.md` Section 8 owns it. Also covered: the mask assets Section 12 consumes are terrain-half atlas ids, and serving as a mask does not make an id undrawable - `0x70..0x7F` and `0xD0..0xD3` are both stencils and placed terrain (`RETRACTIONS.md` R314). Open: no wall-clock rate for the driver-side pass was derived here and the measurement is authoritative over anything that would be; the per-fixture colour-bit breakdown of Section 12.4 and the banner/sail row-swap stage of Section 12.5 are unobserved; the gated half of the five-family divisor (pendulum, standard, clock/bellows) has been captured by nobody yet. Section 5 is rewritten as phase-zero facing decisions with no movement path (R316), and a new Section 13.5 publishes the blocking-presentation contract: autonomous simulation never runs inside a presentation (no exceptions, with the census scope stated), while five named scripted presentations do displace actors or the party by direct placement. |
| Magic/spells/reagents | Complete | `systems/magic.md`; player spell order, parser, dense token/recipe/scene-mask tables, sparse long-incantation display phrase boundary, mix-time-only recipe enforcement including wrong-recipe reagent loss, no charge grant, target-slot refresh, and shared trap-effect handoff, resources, major handler families, combat fields, Dispel Field dungeon live-cell and combat active-object removal semantics, directed wind-cone geometry and actor scan, active effects, shared active-effect counter aging, Negate Time countdown semantics, combat C-Cast interference-source writer/revalidation/clear/persistence lifecycle, Blackthorn's-Castle/Stonegate absorption pre-gates, ordinary world-door Open/Unlock helper behavior, Conjure's weighted animal selector plus independent `0..10` X/Y placement attempts, Swarm's target-cell retry shape, Summon's eight independent random placement probes (**corrected 2026-08-23**: earlier revisions said "ordered eight-cell placement ring"; that is withdrawn, and `catalogs/spell-list.md` has carried the corrected reading since), summon/clone combat-table lifetime with no separate duration counter, shrine linkage, CAST2 shrine/urn active-object pattern separation from party summon spells, the control state a conjured or summoned creature starts in (**corrected, issue #185**: the controlled bit hands a monster-side slot to the player's prompt rather than leaving it AI-driven - R354, which also reverses the earlier R074 withdrawal), monster-spell separation from the party C-Cast dispatcher (**corrected, issue #185**: the driver calls the shared primitives directly and enters no command parser - R353), and the possess/blink/summon-daemon monster-special hook boundary are covered. Equipment counter semantics are delegated to `systems/inventory.md`, visual tile labels to `catalogs/tile-catalog.md`, and ordinary combat AI to `systems/combat.md`. |
| Karma/virtues/shrines | Complete | `systems/karma.md`; virtue order, shrine quest-state machine, Codex urn reading as the ordained-to-Codex-read transition, save-backed scalar moral-standing selector, factory-image seed preservation for the scalar selector, confirmed shrine standing changes, offering/stat-reward boundary, Blackthorn's five-band `KARMA.DAT` verdict selector, Lord British-in-disguise camp-event sixth-record selector, resurrection XP-penalty consumption, TALK moral-standing threshold reads, absence of traced shrine-side `KARMA.DAT` consumption, ruled-out party-gold high-byte storage hypothesis, selected non-shrine scalar deltas for object-taking, town-family chest, town-cannon hit, thankful NPC paths, and toll-milestone gold payment, borrowed-furniture no-debit boundary, conversation profanity and stolen-action warning negative direct-standing-write boundaries, shop-pricing negative boundary, endgame non-use of per-virtue standing, absence of combat-exit/ordinary combat virtue writes, and the boundary that no separate per-virtue numeric save layout is promoted for the traced baseline are covered. Future newly traced action writers should be added with their own signs, magnitudes, and clamp/floor rules rather than inferred from dialogue text or manual-facing virtue expectations. |
| Shops | Complete | `systems/shops.md`; shop text records, main menu flows, table-driven headline pricing, post-transaction random surcharge and shared town/conversation sentinel gate, Talk-entry shop dispatch and caller context, shipped `.NPC` shop-trigger values, horse-trader sale placement (adjacent-cell probe in south/north/east/west order, accepted placement tiles, horse active-object write, no display-stock row), per-shop vendor names and corrected `#`/`$` substitution sources, mounted-party shop entry gate, tavern provision branch (Intelligence-adjusted per-unit price, twenty-five servings per pack, per-unit pay loop, six-record quote pool, table-scraps outcome), arms stock-call literal pool, horse-trader Talk purchase and horse-object placement, shipwright Talk sale trigger, shipwright active-object placement handoff, shipwright Frigate/Skiff payload semantics and duplicate-purchase edges, arms `B` stock-table item-id mapping with all nine scene-local rows and `0xFF` row terminators, arms `S` sell-back inventory browser, arms buy/sell Intelligence formulas and zero-price suppression, reagent compact-menu availability, tavern drink round and Blue Boar fixed-choice price tables, tavern/meal-counter provision purchase and partial-affordability behavior, sage topic/fee/rumour substitution flow and input boundary, shipwright pending-action ownership, karma non-modulation, healer/sanctum treatment flow including The Healers Mission no-price Cure/Heal branch, inn registry marker/clear/death-source/month-counter behavior, and the six per-inn bed cells with the one-tile eastward step on waking and the three early exits that skip it (**issue #190**) are covered. The earlier food-merchant `F`/`S` interpretation has been removed; no standalone provisions merchant is modeled for the analyzed baseline. |
| Endgame | Complete | `systems/endgame.md`; terminal scene setup, normal overlay-export entry, statically resolved dungeon-room/post-combat cleanup callers, stock Doom final-room absorption handoff route, overlay-owned Lord-British-styled two-prompt confirmation dialogue, final-answer-plus-Sandalwood-Box-flag branch semantics, refusal-tableau random local wander, fixed six-window `END.DAT` final narrative presentation, certificate date/elapsed-time output, no-return behavior, and the negative boundary against ordinary Lord British throne-room TALK routing are covered at terminal-state contract depth. Exact character-animation pose mapping, display-helper taxonomy, and endgame resource-slot/panel parity remain presentation QA rather than state-progression blockers. |

## 4. Catalogs

| Catalog | Status | Spec doc |
|---------|--------|----------|
| Tile catalog | Complete | `catalogs/tile-catalog.md`; all five hundred and twelve top-down tile indices have class assignments and storage-domain contracts, including the Sceptre-dissolvable `0x70..0x7F` barrier/field family, resident terrain-query model, visibility propagation ownership boundary, LOOK2 surface/town look-string ownership, the separate packed `DUNGEON.DAT` cell-encoding boundary, vehicles, fields, broad sprite families, dynamic moongate frame composition with no authored effect-frame family, the `0xDC` terrain/Dragon active-object storage-domain split, the `0x80..0x87` fixture-range correction, the completed terrain-half sentinel-row roster with the rule that a sentinel row implies nothing about drawability, the actor-byte-plus-`0x100` collision worked through for the Orc/Ettin/Headless runs, and the boundary that special-trigger tile ids alone do not prove outdoor ascent behavior. Natural-moongate entry is specified by the overworld contract. Per-frame visual attribution, marker-label polish, render-only sentinel naming, field-frame duration checks, and pixel/runtime trace cross-checks are presentation/catalog QA rather than gameplay-spec blockers. |
| NPC roster | Complete | `catalogs/npc-roster.md`; location rosters, public scene/place crosswalk, schedule summaries, AI/mode byte meanings, keyword counts, complete occupied tag byte catalogue, major named NPCs, and the intentional punctuation-only `CASTLE:1` display entry are covered at roster-catalog depth. Full keyword graph integration without dialogue dumping remains quest/conversation catalog validation rather than an NPC roster blocker. |
| Quest graph | Complete | `catalogs/quest-graph.md`; major artifact, word, shard, mantra, shrine/Codex urn, social, utility-item, companion, and endgame dependencies are covered without dialogue text, including the Doom `VERAMOCOR` boundary as mechanically authored Word-of-Power rule data rather than a confirmed NPC-taught clue. The public VM contract covers per-scene branch flags, moral-standing threshold branches, gold-payment gates, global action-letter grants, and label-scoped follow-up prompts; shipped-data reachability comparison and embedded/trailing-record triage are QA/data-authoring aids rather than gameplay-spec blockers. |
| Gazetteer | Complete | `catalogs/gazetteer.md` |
| Spell list | Complete | `catalogs/spell-list.md`; all forty-eight player spells have parser tokens, recipes, scene masks, broad/fixed handler semantics, shared active-effect counter aging, and Negate Time countdown semantics; the resident long-incantation phrase table is identified as a sparse display aid rather than a dense id map; monster special abilities are separated into combat AI, equipment counters belong to inventory/equipment specs, and visual tile labels belong to the tile catalog. |
| Item list | Complete | `catalogs/item-list.md` and `systems/inventory.md`; inventory families, equipment categories, the full forty-eight-row equipment item-id order, equipment slot order, R-Ready stock movement including unequip returns and ammunition readiness gates, equipment base prices, ordinary attack max-damage values, the separate equipped-item weight-statistic table, item-id keyed weapon-dispatch range/effect rows, shared combat attack-routing model, absence of ammunition/thrown/glass item mutation across the traced combat attack stack and separate traced equipment consumers, cached-character-defense ownership rather than live armour-to-defense recomputation, guild commodity prices, herbalist reagent price matrix, healer treatment fees, tavern provision prices, horse and shipwright base prices, inn base/minimum rates, Ring of Invisibility/Ring of Regeneration vanish and combat-side removal behaviour, Amulet/Turning combat-passive semantics, arms-shop equipment item-id mapping, reagents/spell stocks including rare-reagent Search harvest cadence, traced caps for gold, food, keys, gems, torches, spell charges, scrolls, potions, and equipment grants, V-View gem consumption boundary, CAST U-Use dispatch families, scroll counter order and branch gates, potion colour/effect mapping with variation roll, shared EGA/Tandy potion viewport inversion and the complete eight-row blocking sound timing table, Orange sleep-tile/wake lifecycle, Purple combat-record tile replacement, White fixed-grid repaint timing and no-extra-turn boundary, Spyglass routing to the LOOKOBJ full Britannia chunk-map renderer, Sextant coordinate formatting, Pocket Watch hour formatting, Sceptre `0x70..0x7F` barrier-family dissolution, worn-regalia/Black Badge U-Use boundary versus dialogue-branch ownership, Moonstone bury/recovery slots, Wooden Box direct non-endgame boundary, Ankh non-U-Use boundary, normal vehicle terrain predicate ownership, balloon art as a non-live transport boundary, Grapple/Klimb linkage including the legacy magic-powder byte naming collision, Sandalwood Box clue/static castle pickup/endgame flag linkage with object-tile versus inventory-add-code separation, fixed hidden-treasure acquisition ownership, conversation action-letter item grants, and explicit ownership boundaries for dialogue reactions, object visuals, vehicle markers, and opaque save bytes are covered. The White potion's reveal rule is corrected (R318): a full eleven-by-eleven reveal straight from the map with no distance or line-of-sight test, not a squared-distance threshold of 32; the same sweep is the X-Ray spell's body. |
| Monster bestiary | Complete | `catalogs/monster-bestiary.md`; the forty-eight-entry group-banner name table with its twenty-two derived / twenty-six non-derived split and its six `x` placeholders (Section 2.2, **issue #185, 2026-09-02**), class IDs, sprite runs, 48-row class-stat layout and field meanings, full per-row stat values for hostile/special classes, both reserved class gaps as all-zero unnamed rows, HP, reward units, drop caps, Mass Charm thresholds, target-picker filter/fallback behaviour including Doom and Shadow Lord exceptions, ordinary AI action selection (**corrected, issue #185**: the driver calls the shared primitives directly and enters no command parser - R353), teleport-capable movement consumer, possess/blink/summon-daemon special-hook semantics and row assignments, poison/status attack row assignments, turnable-attack row assignments including the Mage non-monster boundary, boss-style vanish-on-death assignments, default drop-marker/framer non-durability boundary, row values for ranged/effect maximum-range/damage-selector and payload bytes, scene-resistance rows, the Gremlin food-theft branch row (**issue #187, 2026-09-03**: renamed from "cast-like branch" and its behaviour corrected - R361), the eleven previously unpublished party/NPC side rows one through eleven with selector one as the melee sentinel (R360), the Mimic pre-gate bypass row, decoded traits, and opaque policy for component bits without independent behavioral consumers are covered. Outdoor random-spawn terrain buckets and arena selection, dungeon room arena selection, placement geometry, sprite-sheet tile verification, and pixel-level special-death visuals are owned by encounter, `.CBT`, tile-catalog, combat, or presentation specs rather than by the class table; spawn counts and early-spawn companion substitutions are class-indexed and already owned here. Future body/retrieval rewards, if any, belong to Search/Get/container rules rather than the class table. |

## 5. Verification Slice

An earlier external first-playable verification slice is summarized in
`NEXT-STEPS.md`. It is not part of this dirty specification workspace, and
agents working here must not create or update any `u5-engine` directories. The
reported slice exercises:

- Decoding `TILES.16` and rendering Lord British's throne room from
  `CASTLE.DAT`.
- Basic movement and collision against location tiles.
- Door interaction.
- Clock/NPC schedule sampling for Lord British's castle data.
- Conversation-envelope loading against `CASTLE.TLK`.

The first run corrected a public spec error by binding Lord British's castle to
`CASTLE:0`. Future implementation verification should remain external to this
dirty workspace; only clean public corrections derived from those results belong
in this specification repository.

## 6. Stop Condition For V1

The v1 visible-MVP spec is considered releasable when:

1. Every non-deferred system, format, and catalog row above has a public
   cleanroom prose document.
2. Every known partial row states its own remaining parity gaps.
3. The first verification slice runs and any resulting public-spec corrections
   are applied here.
4. No spec document contains source code, assembly excerpts, copied decompiler
   output, or raw copyrighted data dumps.

Current assessment: the v1 visible-MVP prose inventory exists, non-deferred
rows point to cleanroom specs, partial rows name their remaining parity gaps,
and the first verification slice has run. Exact-parity work remains in the
declared partial/deferred rows, but those gaps no longer block the first public
cleanroom spec release.
