# Containers and Pickups

## 1. Overview

Ultima V has three ways for the party to obtain loose resources during normal
play:

- `G` Get collects visible pickups, dungeon chests, food/crops, and some
  table objects.
- `O` Open opens unlocked doors and containers, including chests that use a
  container helper after the visible tile check.
- `J` Jimmy unlocks locked containers and doors using key stock, then hands
  successful chest cases into the same container/item machinery.
- `S` Search reveals containers, corpses, buried Moonstones, rare reagents,
  fixed treasures, and secret features. It grants no inventory of its own: a
  container is narrated, and a Moonstone, reagent-free treasure or corpse
  spill is *staged* as an object on the searched cell for a following `G` Get.
  The rare-reagent harvest is the one exception and credits its counter
  directly. *(Corrected 2026-09-12, issue #262: this bullet previously said a
  found hidden object or Moonstone "is granted through the same inventory-add
  path used by pickups and chests" - `RETRACTIONS.md` R477.)*

This spec centralizes the container and pickup contract. Door state, floor
changes, and the detailed lockpick rules live in `doors-and-z-transitions.md`;
this document owns what happens once a gettable object or container result is
ready to become inventory.

## 2. Runtime Stores

The pickup system bridges three stores:

- **Shared party inventory.** Gold, food, keys, gems, torches, reagents,
  equipment counts, scrolls, and quest items are counters or flags in the save
  image. `catalogs/item-list.md` names the item families; `formats/saved-gam.md`
  gives the byte layout.
- **Per-map object table.** The active map has an object list whose entries
  carry a tile/category byte, position, floor/depth, and inventory-add code.
  Surface and town pickups search this table before falling back to raw tile
  classification.
- **Saved Moonstone slots.** Buried Moonstones are tracked in the Gate Travel
  slot block in `SAVED.GAM`, not in the static map files. Search can surface a
  matching slot as a temporary "strange rock" pickup - granting nothing itself
  - and Get invalidates the slot when that pickup is collected, silently.
- **Live map tile buffer.** Some pickups are encoded as mutable terrain or
  furniture cells. Collecting crops, borrowing an object, opening a chest, or
  eating from a table rewrites the live tile byte and marks the visible map
  dirty. These mutations are visit-local unless separately persisted by the
  object layer.

Dungeons differ from surface/town maps: a dungeon chest is identified by the
class nibble of the current dungeon cell. The cell does not store its exact
contents. Contents are generated by the command handler when the chest is
opened or gotten.

Search bridges the same stores through several lookup paths. A matching
object-table entry of the container class goes to the trap narrator of Section
5; this paragraph previously sent a matching entry "through the inventory-add
path", which is withdrawn (`RETRACTIONS.md` R477). If that test misses, Search checks for a corpse at the coordinate,
classifies the live tile to choose a location prefix or hidden-door mutation,
and then runs the saved Moonstone, rare-reagent, and hidden-treasure tables. Ordinary feature results are narration, while hidden-door reveal,
bomb-trap clearing, chest post-processing, and treasure-like results can mutate
the live tile buffer or grant inventory through their owning helpers.

## 3. G-Get

Get operates on the tile in front of the party in overworld and town-family
scenes.

1. The command runs the shared tile-interaction preflight gate. A failed gate
   exits without changing inventory.
2. The handler computes the adjacent target coordinate from the current
   position plus the cached direction step.
3. It scans the current map object table for a matching position. Floor/depth
   must also match, but **only in the low scene band**: in a combat scene a
   record on a different floor byte is still picked up. The filter is the
   record's inventory class byte - accepted when it is below sixteen, or is the
   Moonstone class, the magic carpet class, or one of the four regalia/shard
   classes - while actors, blockers, already-handled entries, and unrelated
   rows at the same coordinate are ignored.
4. On a matching record, that **same** class byte selects the grant arm, and
   the record's auxiliary byte supplies the quantity or the sub-kind. The
   appropriate inventory counter or flag is updated, and ordinary object-table
   pickups clear the consumed slot through the shared slot initializer.
   Moonstone "strange rock" pickups additionally invalidate the associated Gate
   Travel slot after granting the Moonstone. Two class values grant nothing: a
   container record answers `Open_it_first!\n` and a zero class byte reaches
   the dispatcher's default arm and answers `Nothing_to_get!\n`; neither
   clears the slot, marks the party dirty or sets the turn sentinel, all three
   of which every granting arm does.
5. If no object matches, the handler classifies the live map tile. The tile
   fallbacks are borrowing a lit wall torch, picking crops, and eating from a
   reachable laden table. If no fallback applies, Get prints the standard
   nothing-to-get refusal - a second, byte-identical copy of the same text.

Dungeon Get acts on the underfoot dungeon cell, prints its own `Get` row before
looking at anything, and runs **no** acting-member prompt and **no** direction
prompt. A *closed-chest* cell refuses until opened; an open-chest cell is
consumed and rolls generated contents, adding every successful category to the
party inventory; every other cell class refuses. (Corrected 2026-09-12: this
paragraph previously said "Door cells refuse until opened". The refusing class
is the closed chest, and the dungeon cell classes this command recognises are
the two chest classes only.)

**The per-map object filter and the grant selector are one byte.** *(Corrected
2026-09-12, issue #262.)* This document previously said "the accepted per-map
object filter is visual, not the same byte as the inventory-add class code",
and concluded that the non-speaking `CASTLE:0` object slot 31 succeeds
"because the object's tile is in the gettable visual family, not because every
inventory-add code below `0x10` is itself an accepted object-tile tag". Both
halves are withdrawn (`RETRACTIONS.md` R476). The record's class byte is tested
for acceptance and then passed straight to the dispatcher in Section 8, so the
`CASTLE:0` slot carrying class `0x0E` is accepted *because* `0x0E` is below
sixteen, and the Sandalwood Box arm is the arm that class selects. The four
special classes above sixteen - Moonstone, magic carpet, and the four-value
regalia/shard run - are the additions to that range, not a separate visual
space. The class space is the same one the container and chest paths use.

## 4. Chests And Containers

Surface/town chests can be reached through Get, Open, Jimmy, or Search depending
on how they are encoded:

- A visible unlocked or closeable container can be opened. The live cell is
  rewritten to the open-container tile, the visible map is marked dirty, and the
  chest helper determines whether the chest is empty, trapped, or grants
  contents.
- A locked container must be Jimmied first. A successful pick clears the
  matched object's lock/trap flag — preserving its content class — so a later
  Open/container helper enters the chest/content path with no trap armed. A
  failed pick breaks one key and changes nothing at all: the lock still stands,
  the contents are intact, and the container can be attempted again while keys
  remain. There is no "broken-lock" state and no way for a failed pick to
  destroy a container's contents. The formulas and key side effects live in
  `doors-and-z-transitions.md`.
- A too-heavy or otherwise non-openable foreground object refuses before the
  chest helper runs. A non-matching fallthrough can still enter the helper and
  produce a "nothing to open", "can't", trap, empty-chest, or content result
  based on per-map object data.
- A container may be found by Search through the map object table. Search
  announces only the container's assessed trap state (Section 5); it grants no
  inventory and does not open the container. *(Corrected 2026-09-12, issue
  #262: this bullet previously said Search "announces the find and dispatches
  the object's inventory-add code" - `RETRACTIONS.md` R477.)*
- A **corpse** may be found by Search through the coordinate lookup into the
  active object table, after the container scan misses. Class `0x1F` at the
  searched coordinate takes this branch, which prints the shared find preamble
  and then the corpse-search outcome of Section 5; it grants no ordinary
  inventory. This lookup scans active object slots in reverse priority order,
  so the highest matching slot wins when several dynamic records occupy the
  searched coordinate. *(Corrected 2026-09-12, issue #262: this bullet
  previously described the same branch as a "slot-indexed treasure marker"
  find. Search has five result shapes - container, corpse, live-tile prefix,
  hidden door, and the Moonstone/reagent/fixed-treasure scans - and none of
  them is a slot-indexed treasure grant.)*
- A hidden door may be revealed by Search when the searched live tile is the
  hidden-door marker. This path prints the hidden-door find, rewrites the live
  target tile to the revealed variant selected by the current plane, and marks
  the map dirty. It does not run the later Moonstone, rare-reagent, or fixed
  hidden-treasure scans.
- A buried Moonstone may be found by Search through the saved Moonstone slot
  block after live-tile classification. Search creates a visible "strange
  rock" pickup for the matching slot; collecting that pickup grants the
  Moonstone and clears that slot's valid destination marker. One live-tile
  marker skips this Moonstone pass and continues with the reagent and fixed
  hidden-treasure scans.
- A fixed hidden treasure may be found by Search through the resident hidden
  treasure table described in Section 5. This path is independent of the active
  object table at lookup time, so it can represent fixed map secrets that are
  not present as ordinary visible object rows. When a record is accepted, Search
  stages a pickup into an active-object slot at the searched coordinate and
  names the staged object; the later Get path owns the inventory transfer.
- A dungeon chest is an underfoot dungeon cell. Its exact contents are not
  stored in `DUNGEON.DAT`; they are generated at interaction time.

Trap handling is part of the chest helper. A trapped chest prints the trap
result, applies the selected damage or status effect through the shared trap
resolver described in `traps.md`, and then either grants contents or leaves the
chest consumed according to the helper result. The helper passes no trap
flavour: the container is trapped or it is not, and `traps.md` owns the flavour
choice. The surface/town chest content pools below use the chest's low
seven-bit class after any trap bit is removed.

The trapped flag and the locked flag are the same flag, so a container that has
been successfully Jimmied is also disarmed and its later Open is trap-free.
Whichever way the chest is entered, the low seven-bit content class survives
every lock and trap interaction, so container contents can never be destroyed
before they are generated.

When the chest helper opens a matching surface/town object-table chest, it
chooses the party member who opens it through the shared acting-member
selection, and it does so *before* testing whether the chest is trapped. A
prompt is only one of that selection's three outcomes — a combat-class scene and
a set active character both bypass it, and a party with exactly one able member
selects that member silently. `systems/traps.md` § 2.1 owns the priority order
and its consequences, including the case where the selection returns nobody and
the command aborts before any trap can fire.

When the ordinary roster scan finds no Good or Poisoned member, the selector
appends exactly `None!\n`, with no leading newline, member name or `Player: `
prompt of its own. The matching surface/town chest and the dungeon closed-chest
Open path then return without firing a trap, granting contents, changing the
container or charging the chest's standing debit. This refusal applies when
selection is reached; dungeon Get of an already-open chest does not introduce
an acting-member selection. The combat and active-character overrides retain
the priority described in `systems/traps.md` Section 2.1.

Source provenance: fresh original selector and both chest-helper executions
under `u5-decomp/functions/ULTIMA_EXE/` and `u5-decomp/functions/SJOG_OVL/`;
eight isolated no-qualifier cases confirmed the output and unchanged state.

The helper then **clears the matched object record outright** — its kind, its
position, and the byte carrying its lock/trap flag and content class are all
zeroed — marks inventory/status state dirty, and in town-family scenes reduces
the shared moral-standing selector by two units, clamped at zero. Two ordering
details are load-bearing and were re-derived from the shipped binaries on
2026-08-23:

- The clear happens **after** the helper has taken its own local copy of the
  container's flag/class byte and **before** it tests that copy for the trap
  condition. So the trap still fires on this open, and content generation still
  uses the class bits, even though the record no longer exists by then.
- The record lives inside the persisted save image, so the clear is durable. A
  later Open of the same square matches no container and reports that there is
  nothing to open; a trapped surface or town container therefore cannot spring a
  second time. `systems/traps.md` § 4 states the same rule for the trap side and
  records that an earlier revision of that document published this point as an
  open gap on the false premise that the handler only masked the flag in a
  working copy.

If the copied byte's high bit is set, the helper prints the trapped result and
invokes the shared trap-effect resolver in `traps.md` with the selected member as
the target context. The same low seven bits then drive content generation whether
or not the chest was trapped. If no content row from either pool succeeds, the
chest reports empty.

Every successful row from either pool is committed the same way: the engine
acquires a free active-object slot, writes the result family and the rolled
quantity into it, and names the granted object. The slot allocator carries a
shipped quirk that a reimplementation should reproduce rather than tidy away.
It scans the active-object table from the highest index downward and returns
the first index whose class byte is free; when every entry is occupied it
returns index zero as well. The caller reads a returned index of zero as "no
slot available" and abandons that row's grant. Two consequences follow: index
zero of the active-object table can never receive a chest-loot grant even when
it is genuinely free, and rows rolled while the table is full are dropped
silently rather than reported.

The primary pool is evaluated once in the order shown below. A row is eligible
only when its threshold is less than or equal to the chest class. Eligible rows
then roll `1..30` and succeed when the roll is greater than or equal to the
same threshold. Multiple rows can succeed for one chest.

| Order | Result family | Threshold | Quantity or subtype |
|---:|---|---:|---|
| 1 | Food | 7 | Roll `1..2`. |
| 2 | Torches | 7 | Roll `1..2`. |
| 3 | Gems | 15 | Roll `1..2`. |
| 4 | Keys | 9 | Roll `1..2`. |
| 5 | Scroll | 17 | Roll `1..8`, then subtract one for subtype `0..7`. |
| 6 | Potion | 17 | Roll `1..8`, then subtract one for subtype `0..7`. |
| 7 | Gold | 3 | Roll `1..(3 * chest_class)`. |
| 8 | Chest marker | 25 | Roll `1..chest_class` as the staged context value. |

The secondary pool runs after the primary pool. It is not an opaque loot table:
it is the game's **equipment array in save order** — four helms, five shields,
seven armours, twenty-six weapons, three rings, and three amulets — so a pool
index here is directly the save-file equipment index for that item, and the
threshold column is simply a per-item rarity gate laid over that array. The
rows marked disabled are exactly the unique and quest items, which no chest can
ever produce.

The pool performs `floor(chest_class / 2) + 1` independent attempts. Each
attempt first chooses a pool index uniformly from `0..47`, then uses the
selected row's threshold in the same two gates: threshold must be less than or
equal to the chest class, and a `1..30` roll must be greater than or equal to
the threshold. On success, the pool index itself is the equipment subtype passed
to the inventory-add path. Rows marked disabled never succeed for any chest
class.

| Pool index | Result family | Threshold |
|---:|---|---:|
| 0 | Helm | 10 |
| 1 | Helm | 10 |
| 2 | Helm | 15 |
| 3 | Helm | 20 |
| 4 | Shield | 10 |
| 5 | Shield | 15 |
| 6 | Shield | 20 |
| 7 | Shield | 28 |
| 8 | Shield | Disabled |
| 9 | Armour | 15 |
| 10 | Armour | 15 |
| 11 | Armour | 20 |
| 12 | Armour | 20 |
| 13 | Armour | 20 |
| 14 | Armour | 24 |
| 15 | Armour | Disabled |
| 16 | Weapon | 5 |
| 17 | Weapon | 10 |
| 18 | Weapon | 10 |
| 19 | Weapon | 10 |
| 20 | Weapon | 10 |
| 21 | Weapon | 10 |
| 22 | Weapon | 10 |
| 23 | Weapon | 10 |
| 24 | Weapon | 15 |
| 25 | Weapon | 15 |
| 26 | Weapon | 15 |
| 27 | Weapon | 10 |
| 28 | Weapon | 15 |
| 29 | Weapon | 10 |
| 30 | Weapon | 20 |
| 31 | Weapon | 20 |
| 32 | Weapon | 20 |
| 33 | Weapon | 20 |
| 34 | Weapon | 20 |
| 35 | Weapon | Disabled |
| 36 | Weapon | 23 |
| 37 | Weapon | 23 |
| 38 | Weapon | 23 |
| 39 | Weapon | Disabled |
| 40 | Weapon | Disabled |
| 41 | Weapon | Disabled |
| 42 | Ring | 23 |
| 43 | Ring | 23 |
| 44 | Ring | 23 |
| 45 | Amulet | 23 |
| 46 | Amulet | 15 |
| 47 | Amulet | Disabled |

## 5. Fixed Search Tables

Search has a resident fixed-treasure database separate from the map object
table. The complete public table is `hidden-treasures.md`. It contains 113
records split into parallel semantic columns:

| Column | Meaning |
|---|---|
| Pickup class | The object-name/pickup class staged into the active-object slot and used for the Search narration. |
| Pickup state | The per-record state byte staged with the active-object slot; Get later consumes this as the found-item context. |
| Scene | The town, keep, dungeon, or overworld scene in which the record can match. |
| Floor/depth | The local floor or dungeon depth that must match the party's active Z value. |
| X coordinate | The searched tile's X coordinate. |
| Y coordinate | The searched tile's Y coordinate. |

When Search reaches this table, it compares the searched coordinate and current
scene state to each record in order. On a normal match, the engine checks a
save-backed already-found bitmap. If the record is still clear, the bit is set,
an active-object slot is acquired, the pickup class/state and searched
coordinate are written into that slot, the inventory/status dirty hint is
raised, the staged object is named, and Search succeeds. Repeating the same
Search later skips the already-found record and continues scanning.

Three records in the table have extra gates instead of the ordinary one-shot
bitmap rule:

| Record family | Extra rule |
|---|---|
| Zero-key cache | Requires the party to own no keys and the target tile not to be occupied by an NPC. The gate reads the ordinary Keys counter. |
| Daily cache | Can fire once per in-game day; on success it stores the current day in its cooldown byte, which lives inside the special/quest-item band rather than being a field of its own. |
| Single-use / occupancy-gated cache | Requires no NPC on the searched tile and requires the party to carry none of the item the record grants. The gate reads that item's ordinary equipment-inventory counter, so the grant path never writes it — taking the granted item does. It is not a separate cookie. |

These special cases still use the same final pickup-staging and found-object
narration path once accepted. Implementations should preserve the table's
matching order, already-found bitmap, pickup staging, and special gates rather
than inventing a different hidden-object system.

Before the Moonstone, rare-reagent, and fixed-treasure scans run, surface/town
Search classifies the live tile that was present at the searched coordinate.
For most entries this classification only chooses the location phrase that
precedes any later found-object narration; it does not by itself grant
inventory. Compatible implementations should classify the original live tile
read before the active-object lookup, not any non-treasure active-object class
returned by the coordinate lookup.

| Live tile | Search classification |
|---|---|
| Active object class `0x1F` at the target coordinate | The **corpse branch** (Section 5, corpse-search bullets): class `0x1F` is the moldy-corpse class, so this is an immediate corpse find and the later live-tile table is skipped. *(Corrected 2026-09-12, issue #262: earlier revisions called this an "immediate slot-indexed treasure find"; there is no slot-indexed treasure branch in Search.)* |
| `0x2B` | Location prefix: stump. |
| `0x4E` | Hidden-door find; reveal the live tile and stop. |
| `0x4F` | Location prefix: wall. |
| `0x5A` | Location prefix: shelf. |
| `0x5C..0x5D` | Location prefix: bookshelf. |
| `0xA1` | Location prefix: well. |
| `0xA5` | Location prefix: desk. |
| `0xA6` | Location prefix: barrel. |
| `0xA8` | Location prefix: vanity. |
| `0xAB..0xAC` | Location prefix: under bed. |
| `0xAD` | Location prefix: dresser. |
| `0xAF` | Location prefix: trunk. |
| `0xB2` | Location prefix: brazier. |
| `0xBC` | Location prefix: fireplace. |
| `0xDC` | Generic find prefix; skip the Moonstone scan, then continue with rare-reagent and fixed hidden-treasure scans. |
| Other tiles | Generic find prefix, then continue through Moonstone, rare-reagent, and fixed hidden-treasure scans. |

The top-down hidden-door success prints exactly
`\nThou dost find\na hidden door!\n` after the direction/acting-member
exchange. It uses the generic find prefix, not the wall-location prefix
of tile `0x4F`, and adds no `Success!` or other secret-door sentence.
Dungeon Search is distinct: its `You find:\n` preamble is followed by
`A hidden door!\n` for the packed hidden-door family, with capital A.
Source provenance: freshly traced top-down and dungeon Search output under
`u5-decomp/functions/SJOG_OVL/` and `u5-decomp/notes/`, issue #225.

Search also has a small fixed reagent-harvest table for rare reagents. It
contains three fixed overworld locations:

| Harvest point | Overworld X | Overworld Y | Reagent counter |
|---|---:|---:|---|
| Mandrake point A | 182 | 54 | Mandrake Root |
| Mandrake point B | 97 | 165 | Mandrake Root |
| Nightshade point | 44 | 137 | Nightshade |

A harvest succeeds only when the party searches the matching coordinate at
midnight and the same harvest point has not already been harvested on the
current in-game day. On success, the engine rolls a quantity in the 2..15
range, adds that amount to the corresponding reagent counter with a 99-unit
cap, records the current day as that point's cooldown cookie, and prints the
"sprigs of" reagent narration. Searches at the right coordinate but the wrong
hour, or repeated on the same day, fall through as no harvest.

Each harvest point owns its own cooldown cookie, and the three cookies are
durable save state (`formats/saved-gam.md` Section 10), so the once-per-day
limit survives save and reload. The cookie stores a day-of-month only; nothing
clears it at ordinary midnight, it simply stops matching when the day advances.
The 28-day month rollover does zero all three (`systems/time.md` Section 8).
They start at zero on a new game, which matches no calendar day, so all three
points are harvestable at the first midnight of a fresh game.

Search has a separate trap/detail narrator for per-map object slots that carry
trap-class metadata. **The slot must be of the container class**; Search's
object branch tests for that one class and reaches nothing else. The slot
metadata has two semantic parts: a trappable bit and a low difficulty value.
Search combines that difficulty with the acting member's **Intelligence** - the
"trap-detection stat family" named below is that one field - and a uniform
`1..30` roll. This creates a detection bit, not a trap effect:

| Slot metadata | Detection threshold |
|---|---|
| Not marked trappable | `(30 - member trap-detection stat) / 2`. |
| Marked trappable | `(difficulty - member trap-detection stat + 30) / 2`. |

The intermediate threshold is the original unsigned word quantity halved by
one bit, so out-of-range stat or difficulty values should preserve the
wraparound edge behavior rather than clamping before the roll comparison.

The detection bit is set when the roll is greater than or equal to the
threshold. A cleared bit represents a missed or uncertain reading. The visible
result is:

| Condition | Visible result |
|---|---|
| Trappable slot and detection bit clear | No trap. |
| Trappable slot, detection bit set, and difficulty below `10` | Simple trap. |
| Trappable slot, detection bit set, and difficulty above `20` | Complex trap. |
| Trappable slot, detection bit set, and difficulty `10..20` | Generic trap. |
| Non-trappable slot and detection bit set | No trap. |
| Non-trappable slot and detection bit clear | Generic trap false-positive. |

The four stored lines are `no_trap!\n`, `a_simple_trap!\n`,
`a_complex_trap!\n` and `a_trap!\n`, printed on the row after the Search
preamble; `commands.md` Section 5.8 gives the whole transcript. Because the
halving is unsigned, an Intelligence above thirty underflows the untrapped
threshold and makes that roll fail every time, so a very clever member reports
a phantom trap on **every** untrapped container. Whether an Intelligence above
thirty is reachable in ordinary play is not settled here; it decides whether
that arm is a corner case or the normal outcome.

This path is a detection/classification narrator. It does not by itself define
the shared trap effect once a trap is sprung; the damage/status resolver belongs
to `traps.md`. The coordinate probe that reaches this narrator is the active
object lookup described in `systems/active-objects.md`: it returns the topmost
matching object slot's tile/type byte, after which Search owns the feature
narration or pickup-staging branch.

## 6. Dungeon Chest Contents

Dungeon chest contents are generated when the party uses Get while standing on
an already-open dungeon chest cell. The command reads the underfoot dungeon
cell:

| Underfoot cell class | Result |
|---|---|
| Closed chest | Refuses with the must-open-first message. |
| Open chest | Consumes the chest and runs the reward generator. |
| Any other class | Refuses because there is no chest here. |

Consuming the open chest rewrites only the loaded dungeon image for this visit.
The rewrite clears the chest class and preserves the dungeon visit-marker bit,
so an already-looted chest becomes passage-like for the rest of the current
dungeon entry. The on-disk dungeon record is not patched.

**The generator is the whole contract. There is no authored per-chest override.**

Nothing per-chest, per-room, per-level or per-coordinate is consulted. The
generator's complete input set is: the party's dungeon depth; the party's X and Y
**used only to index the underfoot cell**; the cell byte itself, which is masked
so that its low nibble is discarded and cannot reach content generation; three
fixed seven-entry tables in the resident data image; and the shared uniform
random helper.

Two structural facts make that negative safe rather than merely unobserved.
**The routine contains no pointer load at all** - every memory operand is an
absolute-displacement form, so there is no table that could be reached
indirectly and no place an authored index could enter. And **chest identity is
not available downstream either**: the grant dispatcher receives three values
only, none of which carries a position, a depth or a chest id, so it could not
consult per-chest data even if such data existed.

An implementation carrying an authored per-chest content sidecar has no
counterpart for it in the original and should retire it. This is scoped to the
container this section specifies - the underfoot dungeon-cell chest reached by
Get while standing in a dungeon corridor - and says nothing about other
container kinds.

The reward generator is table-driven. It iterates seven reward rows in order.
Each row has a gate threshold, a quantity/subtype rule, and an inventory family
to grant when the row succeeds.

For each row, the first roll is uniform in `1..(4 * dungeon_depth + 4)`, where
depth is the current dungeon level index (`0..7`). If the row's threshold is
greater than that roll, the row grants nothing. Otherwise, the row is awarded
and dispatches once through the shared inventory-add path. Multiple rows can
succeed for a single chest, so a chest can grant several item families.

The seven rows are:

| Row | Gate threshold | Quantity or subtype rule | Inventory result |
|---:|---:|---|---|
| 0 | 2 | Roll `1..31`. | Food. |
| 1 | 4 | Use the shared one-based random helper with upper bound `8 * dungeon_depth`. | Gold. |
| 2 | 5 | Roll `1..3`. | Keys. |
| 3 | 10 | Roll `1..3`. | Gems. |
| 4 | 20 | Roll `1..3`. | Torches. |
| 5 | 25 | Roll random subtype `0..7`. | One potion of that subtype. |
| 6 | 25 | Roll random subtype `0..7`. | One spell scroll of that subtype. |

**What each row prints.** *(Added 2026-09-12, issue #262.)* The handler prints
`Get`, then on an open-chest cell the three-row preamble `contents` / `of chest`
/ `You find:`, then one row per row that fires, in the table's order. The
quantity rows print the decimal count first and then a name fragment that begins
with a space, which is why the rendered row reads as one phrase:

| Row | Rendered row | Notes |
|---|---|---|
| Food | `31 food!` | no singular/plural variation |
| Gold | `56 gold!` | no singular/plural variation |
| Keys | `3 keys!` / `1 key!` | `s!` above one, `!` otherwise |
| Gems | `3 gems!` / `1 gem!` | same pair as keys |
| Torches | `3 torches!` / `1 torch!` | `es!` above one, `!` otherwise |
| Potion | `A white potion!` | colour word by subtype |
| Scroll | `A scroll: AT!` | rune word by subtype |

The singular and plural suffixes are **per-row shipped strings, not one shared
pair**: keys and gems take the `s!` form, torches takes the `es!` form, each
takes its own `!` form when singular, and food and gold have no suffix fragment
at all.

The eight potion colours are blue, yellow, red, green, orange, purple, black and
white, taken from the first half of a sixteen-entry word table using the subtype
directly. The eight scroll rune words are `VL`, `RH`, `IS`, `IA`, `IQW`,
`KXC`, `IMC` and `AT`, taken from the second half of the same table with the
subtype masked to three bits — **three of the eight are three characters long, so they
are not all rune pairs**. The rune word alone is drawn from the game's **second
character set**: the handler switches character sets immediately before printing
it and switches back immediately after, so an implementation that renders it in
the ordinary text font does not match the original screen. The surrounding
`A scroll: ` and `!` are ordinary text.

**A chest whose rows all fail prints the preamble and nothing else.** There is
no "empty" line on this path: `Get` / `contents` / `of chest` / `You find:` is
the complete transcript of an empty dungeon chest.

**Which rows can fire at all is a function of depth**, because the gate roll's
upper bound scales with the depth index. Executed at every depth with the gate
roll forced high: food can fire at every depth, gold from depth index 0, keys
from 1, gems from 2, torches from 4, and the potion and scroll rows only from 6.
On stock data only the single deepest shipped chest can ever produce torches, a
potion or a scroll.

The gold row deliberately passes lower endpoint `1` and upper endpoint
`8 * dungeon_depth` to the shared range helper. At `dungeon_depth == 0`, this
becomes an invalid `1..0` range. If the row's gate succeeds on that level, the
original helper consumes one PRNG advance and then reaches its zero-width
integer-division edge. Compatible implementations should preserve that behavior
rather than silently clamping the bound to one. The observable point at which it
happens is **part-way through the transcript**, after the food row has already
printed. The shipped dungeon map places no chest on a top level, so the branch is
unreachable from stock data — but it is real, and a re-implementation has to
define it rather than inherit it.

## 7. Tile Consumables

Some Get results are not object-table entries:

- **Borrowed lit fixtures.** The two **wall-torch** tiles `0xB0` and `0xB1`,
  and no other cell, can be "borrowed". *(Corrected 2026-09-12, issue #262:
  this bullet, `commands.md` Section 7 and `karma.md` Section 8 all called the
  branch "furniture" or "a table item"; the laden tables are the *eating* case
  and these two ids are the borrow case - `RETRACTIONS.md` R478.)* The cell is
  rewritten to the plain floor tile `0x44` from any approach direction, the
  line `Borrowed!\n` is printed, the theft sound plays, and the party's torch
  counter is set to 100 counter units — borrowing a lit fixture is a light
  source, not an inventory item, and it consumes no carried torch. See
  `systems/lighting.md` for the counter's decay. **This identification is not
  settled:** the store is unconditional and its downstream reader was not
  followed, and a competing reading of the same byte as a theft/alert counter
  is open; see `OPEN-QUESTIONS.md`. The branch is **not**
  stateless: it overwrites the party-dirty byte outright rather than setting a
  bit, and in non-combat scenes it calls one further shared resident routine
  before printing. It does not debit the shared moral-standing selector; crop,
  table-food, and town-family chest handling own the confirmed Get-side
  standing penalties.
- **Crops.** Crop cells `0x2D` can be picked from any direction. The cell
  changes to the plowed-field tile `0x2C`, the line `Crops_picked!\n` is
  printed, one unit is credited to the shared party food word with the 9999
  cap, the shared moral-standing selector is debited when nonzero, the
  party-dirty bit is set, and the turn sentinel is set.
- **Food on tables.** The three laden-table cells can be eaten only from the
  side the food is on. A valid reach prints `Mmmmm...!\n`, rewrites the table
  cell, and runs the same grant tail as crops — one food with the 9999 cap, the
  party-dirty bit, and the moral-standing debit when that byte is nonzero. An
  invalid reach prints `Can't_reach_plate!\n` and mutates nothing.

  All three variants are directional, on the **reached** cell's offset from the
  party:

  | Target tile | Successful Get directions | Resulting tile |
  |-------------|---------------------------|----------------|
  | `0x9A` | Only the vertical direction with `dy = +1` | `0x95` |
  | `0x9B` | Only the vertical direction with `dy = -1` | `0x95` |
  | `0x9C` | Vertical directions with `dy = -1` or `dy = +1` | `0x9A` for `dy = -1`, `0x9B` for `dy = +1` |

  Horizontal reaches (`dx = -1` or `dx = +1`) are invalid for all three. The
  command prints the cannot-reach-plate feedback and leaves the tile and food
  counters unchanged. `0x9C` tests only the horizontal deltas, so a zero step
  would grant without any rewrite; whether the direction prompt can deliver a
  zero step to Get is not established.

  **The grant tails are not identical.** The turn sentinel is set by the crop
  arm and by the `0x9A` arm only; the `0x9B` and `0x9C` arms rejoin the shared
  tail below that store and leave the sentinel untouched. `Mmmmm...!\n` and
  `Can't_reach_plate!\n` each exist as three byte-identical stored copies, one
  per dispatch site.

  Eating is reached only through `G` Get. The town-family per-turn underfoot
  handler dispatches the trapdoor family, the burning family and poison
  terrain, and never inspects a laden table; that negative is scoped to the
  town-family step handler, which is the one that was read.

These are live-map mutations. They should not be written back to the static map
files.

## 8. Inventory-Add Contract

All pickup/container paths converge on an inventory-add dispatcher. Its public
contract is:

- Decode the found-item category or item code.
- Increment the matching shared inventory counter or set the matching quest-item
  flag.
- Clamp or refuse according to that counter's in-game cap where a cap is known.
- Mark inventory/status dirty so the Z-stats and side panels can be refreshed.
- Print the item-result narration for quantities that are visible to the
  player.

Known inventory families reached from pickups and containers are gold, gems,
torches, food/grain, keys, Moonstones, equipment/item categories,
spell-scroll-like results, and special quest/item flags. `catalogs/item-list.md`
is the naming catalog; this spec only defines how pickup results enter that
inventory model.

The found-item class code maps to these public result families:

| Class code | Result family | Inventory effect |
|---:|---|---|
| `0x01` | Must-open-first refusal | Prints the closed-container refusal and does not mark inventory dirty. |
| `0x02` | Gold | Adds the supplied quantity to the party's gold counter, capped at the normal `9999` gold maximum. |
| `0x03` | Potion | Adds one potion of the supplied potion subtype. Potion ids `0..7` follow the colour order in `catalogs/item-list.md`; each colour counter caps at `99`. |
| `0x04` | Scroll or HMS Cape plans | Adds one spell scroll of the supplied spell subtype, capped at `99` per scroll counter, or sets the HMS Cape plans flag when the special plans subtype is supplied. Normal scroll grants use the low three bits of the supplied subtype to select the printed scroll label. |
| `0x05`, `0x06`, `0x09..0x0C` | Equipment | Adds carried equipment stock by the supplied equipment id, capped at `99` per equipment counter. Arrows and Quarrels grant five units per award; other equipment grants add one unit. The canonical equipment id/name table lives in `catalogs/item-list.md`. |
| `0x07` | Key | Adds the supplied key quantity, capped at `99`. Ordinary subtypes add to skeleton keys. Marked odd-key subtypes print odd-key narration, strip the marker bit for the displayed quantity, and add to the skull/special-key counter instead of the ordinary key counter. |
| `0x08` | Gem | Adds the supplied gem quantity. |
| `0x0D` | Torch | Adds the supplied torch quantity, capped at `99`. |
| `0x0E` | Sandalwood Box | Sets the save-backed Sandalwood Box flag used by the endgame. |
| `0x0F` | Food / grain | Adds the supplied food quantity to the food counter. |
| `0x19` | Moonstone | Sets the Moonstone ownership flag for the supplied moonstone slot. |
| `0x1B` | Magic Carpet | Grants the magic-carpet ownership state; one scene-specific acquisition path also raises a quest/event marker. |
| `0xB4` | Shadowlord shard | Writes the no-quantity marker `255` into the Falsehood, Hatred or Cowardice shard flag for shard subtypes `0..2`, and into no other byte; the U-Use row is then uncounted (`systems/inventory.md` Section 4.5). The subtype is **masked to two bits**, not validated: subtypes congruent to 0, 1 and 2 grant a real shard and print its line, and subtypes congruent to 3 write the unused byte after the third shard flag, printing the Cowardice line while granting nothing usable. See `catalogs/item-list.md` Section 8. |
| `0xB5` | Crown of Lord British | Sets the Crown ownership flag and runs the accompanying regalia acquisition side effects. |
| `0xB6` | Sceptre of Lord British | Sets the Sceptre ownership flag. |
| `0xB7` | Amulet of Lord British | Sets the Amulet-of-Lord-British ownership flag. |
| Other values | No pickup | Prints the nothing-to-get refusal and leaves inventory unchanged. |

The class code in that table is the record's own class byte - the same byte
`G` Get tests for acceptance (Section 3), not a second field. The exact printed
line for every row is published in `commands.md` Section 5.8, including the
singular/plural stems, the two lookup tables for potion colours and scroll
mnemonics and the asymmetry between them: the scroll index is masked to three
bits and the potion index is not, so a potion sub-kind of eight or more reads
past the colour words into the mnemonics.

For all successful ordinary grants, the dispatcher marks the inventory/status
view dirty and records that the party obtained something this turn. The
must-open-first branch, torches, food, and the Sandalwood Box use custom
epilogues with equivalent user-visible inventory effects rather than the shared
ordinary-grant epilogue.

## 9. Consumed Object Persistence

Inventory changes persist because the shared inventory counters live in
`SAVED.GAM`. Object-table changes persist when they are reflected through the
active-object/object-overlay save path. Live tile-buffer rewrites inside a
location usually do not persist across leaving and re-entering that location.

The visible consumed-object rules are:

- **Per-map object-table pickups.** A successful object-table grant that carries
  an ordinary slot index clears the consumed object's first six record fields in
  the live object table. The last two working bytes are outside this clear
  helper's contract and remain caller-owned scratch. The cleared slot no longer
  matches later Search/Get/Open/Jimmy scans in the current loaded object layer.
- **Surface/town object-table chests.** The chest helper uses the same slot-clear
  path after committing the chest result. Trap and combat chest branches can
  also mark a related combat/effect record as claimed, but the ordinary
  non-combat persistence rule is still "clear the consumed object-table slot."
- **Corpse searches.** A corpse search splits on a single roll before anything
  is narrated, and the fate of the corpse slot follows that split, not the
  narration. The majority arm -- seven outcomes in eight -- **clears the corpse
  slot first**, and only then rolls for what the search is said to have turned
  up. Plague is one rare result inside that already-cleared arm, and the
  several "nothing"-style results are the rest of it, so plague and every
  "nothing"-style narration alike leave the slot cleared and stage neither food
  nor gold. Only the minority arm -- one outcome in eight -- leaves the slot in
  place and rewrites it into a later food/gold pickup; the eventual pickup then
  follows the ordinary object-table grant rule above. *Corrected:* an earlier
  revision had this backwards, attributing the slot clear to the plague outcome
  alone and the rewrite into a pickup to every non-plague search.
- **Corpse-search scope and victim.** This branch belongs only to a
  surface/town Search whose active-object lookup finds a moldy corpse. A
  rotting body does not enter it. Before the lookup, Search chooses the
  searching member through the shared acting-member selection specified in
  `systems/traps.md` § 2.1; cancellation aborts before any corpse roll. That
  selected slot is also the Plague victim.
- **Corpse-search odds.** Every roll below is over a **closed** range - both
  bounds are attainable - which is the detail most likely to be mis-implemented
  as an exclusive upper bound.

  | Roll | Outcome |
  |---|---|
  | The arm split | **1 in 8** takes the minority arm and stages a pickup; the other **7 in 8** clear the slot. |
  | Inside the minority arm | **1 in 4 food, 3 in 4 gold.** |
  | Inside the cleared arm | Plague on **1 in 32**. |

  An earlier revision of this section gave the contents split as one third
  against two thirds, and the plague chance as one in thirty-one; **both figures
  are withdrawn**. Each was an inclusive/exclusive off-by-one on a closed range.

  The exact branch tree and shared gameplay-PRNG consumption are:

  | Stage or completed path | Inclusive range and predicate | Shared draws consumed |
  |---|---|---:|
  | Arm split | Draw `0..7`; zero stages contents, while `1..7` clear the slot. | 1 |
  | Contents | Draw `0..3`; zero stages food and `1..3` stage gold. Then draw the staged quantity in `1..3`. | 3 |
  | Cleared-arm Plague | After clearing the slot, draw `0..31`; exactly `19` selects Plague. | 2 |
  | Cleared-arm ordinary narration | When the preceding draw is not `19`, first draw an upper bound in `0..3`, then draw the narration selector in `0..upper_bound`. | 4 |

  Plague therefore has unconditional probability `7/256` per moldy-corpse
  Search. Conditional on reaching ordinary narration, selectors zero through
  three have exact probabilities `25/48`, `13/48`, `7/48`, and `3/48`.
  They are not equal quarters.
- **Corpse-search transcript.** Search prints `Thou dost find` followed by a
  line break before entering this branch. The branch then appends exactly one
  of these lines, with no member name or intervening line:

  | Selector | Exact appended line |
  |---:|---|
  | Plague | `Plague!` |
  | Ordinary `0` | `nothing!` |
  | Ordinary `1` | `worms!` |
  | Ordinary `2` | `guts!` |
  | Ordinary `3` | `a bloody pulp!` |
  | Contents food | `food!` |
  | Contents gold | `gold!` |

  Every listed line ends with one line break.
  The four ordinary lines are presentation only and stage neither food nor
  gold.
- **Plague consequence.** After printing its line, Plague plays the blocking
  PC-speaker random-rumble effect with step `40`, total `3000`, and maximum
  pitch `500`: 75 short frequency fragments, each in the inclusive
  `100..500 Hz` range. The rumble uses the audio sequence rather than consuming
  more shared gameplay-PRNG draws. Turning sound off suppresses speaker output
  but preserves all 75 timing iterations and audio-sequence advances. When the
  rumble finishes, the game directly replaces the selected member's status
  with Poisoned and requests a full stats-panel repaint. There is no
  current-status gate, damage roll, HP change, or active-member change. Normal
  selection supplies an in-party member; an imported status admitted by the
  active-member override is overwritten with Poisoned as well. The repaint is
  deferred to the enclosing mode's next refresh boundary rather than called
  inside the Plague branch. Literal forced invocation with an in-range Dead or
  Sleeping slot would also overwrite it; an out-of-range slot is a memory-unsafe
  call edge because this helper performs no local bound check. Neither case is
  reachable through normal Search selection.
- **Corpse-search completion.** The contents arm marks the inventory/status
  view dirty and refreshes the full stats panel immediately after staging its
  food or gold record; it still changes no inventory counter until a later
  pickup, and it plays no sound. Ordinary cleared-arm narration requests no
  stats repaint and also plays no sound. Every completed branch returns through
  Search, and the resident command dispatcher retains its normal-action result,
  so the current mode performs one ordinary consumed-action tail. The helper
  itself does not move
  the party, advance time separately, save a file, or enqueue another action.
- **Combat-created body/retrieval slots.** The ordinary post-combat
  trigger-slot reconciler can rewrite a restored `0x2C..0x2F` body-family
  active-object slot into the same broad body/retrieval family consumed by
  Search/Get. Once the player searches or gets that object, the corpse/body
  reward rules above own the food/gold/plague staging; combat itself does not
  grant those contents directly, and combat exit does not grant party gold,
  karma, or a separate victory bonus.
- **Fixed hidden treasures.** Ordinary records are one-shot through the
  save-backed found bitmap described in Section 5. Once the bit is set, later
  searches skip that record even if the temporary active-object staging slot has
  already been cleared.
- **Moonstones.** A found Moonstone is staged as a temporary strange-rock pickup,
  but durable ownership lives in the saved Gate Travel slot. Collecting the
  pickup grants the Moonstone and invalidates the matching saved slot so the
  same buried stone cannot be found again.
- **Live map tile consumables.** Crops, borrowed wall-torch cells, table
  food, hidden-door reveals, and dungeon chest cells persist by rewriting the
  loaded tile buffer, not by clearing an object-table slot. Surface/town tile
  rewrites are visit-local unless another owning system writes the changed map
  image to disk; dungeon chest consumption rewrites the loaded dungeon image
  while preserving the visit marker.

Modern implementations should therefore separate:

- permanent inventory grants;
- persistent object-layer changes, if any;
- visit-local tile rewrites used for feedback and collision;
- static asset data, which remains read-only.

## 10. Hooks Into Other Systems

- **Commands.** `commands.md` owns dispatcher routing for `G`, `S`, `J`, and
  `O`.
- **Doors and Z transitions.** `doors-and-z-transitions.md` owns lock states,
  door opening, auto-close, and both of Jimmy's key/lock rolls, including the
  fact that a successful pick on a container clears the lock/trap flag while a
  failed pick leaves the container untouched.
- **Dungeon mode.** `dungeon-mode.md` owns dungeon cell classes and the fact
  that dungeon Get/Open act on the underfoot cell.
- **Items.** `catalogs/item-list.md` names item families and records which use
  effects remain open.
- **Save format.** `formats/saved-gam.md` owns shared inventory counters and
  the object-overlay companion files.
- **Active objects.** `active-objects.md` owns map objects and dropped active
  entries.
- **Hidden treasures.** `hidden-treasures.md` owns the complete 113-record fixed
  Search table.

## 11. Completed Boundaries

- **Dungeon chest generator.** The seven-row generator, thresholds,
  quantity/subtype rules, inventory families, multi-row awards, visit-local
  chest consumption, and depth-zero gold-row helper edge are specified.
- **Full fixed-search tables.** The fixed hidden-treasure scan shape is now
  specified, including one-shot and special-gated records, and the rare-reagent
  harvest table is specified. The complete 113-record hidden-treasure table is
  specified in `hidden-treasures.md`.
- **Inventory subtypes.** Potion, scroll, odd-key, shard, ammunition bundle
  grants, and ordinary counter caps are now public. No container-owned
  inventory subtype gap remains.
- **Corpse-search branch.** The exact chained rolls and draw counts, output
  lines, Plague status and rumble consequence, repaint cadence, slot mutation,
  and ordinary action continuation are specified in § 9.
- **Search trap/feature table.** Surface/town Search distinguishes regular
  treasure, per-object simple/complex/generic trap narration, hidden doors,
  fixed furniture/location prefixes, and empty feature descriptions, and
  `traps.md` now specifies the shared party-effect resolver. The per-object
  trap narrator's threshold formula and surface live-tile fallback table are
  now public; the shared trap-effect distribution and surface/town chest
  content pools are also public. The former Search trap-probe uncertainty is
  resolved as an active-object coordinate lookup whose returned tile/type byte
  drives the branch table.
No container-specific open work remains for consumed-object persistence,
ordinary counter caps, or inventory-add subtypes.

## 12. Sources

This cleanroom spec was derived from private analysis notes. It intentionally
does not reproduce decompiled code, assembly, or raw data dumps.

- `u5-decomp/functions/SJOG_OVL/`.
- `u5-decomp/functions/SJOG_OVL/` (filename is
  historical; the note's 2026-08-22 banner withdraws the "trap" reading and
  establishes the routine as the object-slot grant).
- Source provenance: the per-row commit through an acquired active-object slot,
  the downward scan of the slot table, and the index-zero collision that makes
  slot zero unreachable and silently drops rows when the table is full --
  re-derived on 2026-08-22 and recorded in the banner and addendum on
  `u5-decomp/functions/SJOG_OVL/` (filename is
  historical; the routine is the slot allocator, not a party check).
- `u5-decomp/functions/SJOG_OVL/`.
- `u5-decomp/notes/` — source
  provenance for the retraction of the "broken-lock" state, the guarantee that
  a failed pick preserves a container's contents, the identification of the
  secondary pool as the equipment array, and the absence of any caller-side
  trap-selection table.
- `u5-decomp/functions/SJOG_OVL/`.
- Source provenance: the corpse-search arm structure -- the majority arm's
  slot clear happening ahead of the plague-or-narration roll, and the minority
  arm being the only one that stages a pickup -- re-derived on 2026-08-22 from
  the shipped overlay and recorded in the banner and Behavior section of
  `u5-decomp/functions/SJOG_OVL/`.
- Source provenance for the 2026-08-27 closure of issue 175: the exact chained
  rolls and draw counts, transcript order, selected-member status overwrite,
  stats-panel refresh cadence, rumble parameters, normal-action continuation,
  and negative side-effect census were derived from private analysis in
  `u5-decomp/functions/SJOG_OVL/`, `u5-decomp/functions/ULTIMA_EXE/`, and
  `u5-decomp/notes/`.
- `u5-decomp/functions/ULTIMA_EXE/`.
- `u5-decomp/formats/`.

- **Issue #262, the dungeon-object pass (2026-09-12).** The dungeon O-Open and
  G-Get transcripts and their prefix rules, the chest lifecycle and its trap
  sub-type bits, the Open spell's underground arm, the dungeon chest reward
  generator's emitted rows and depth thresholds, and the negative that no
  reachable dungeon state places an active object were re-derived from private
  analysis in `u5-decomp/notes/`, `u5-decomp/functions/DUNGEON_OVL/`,
  `u5-decomp/functions/SJOG_OVL/`, `u5-decomp/functions/CMDS_OVL/`,
  `u5-decomp/functions/CAST_OVL/` and `u5-decomp/functions/ULTIMA_EXE/`, with
  the word, gate and quantity tables read back from the shipped data file.

- **Issue #262 follow-up, the surface Get/Search pass (2026-09-12).** The single
  class byte that both admits a record to `G` Get and selects its grant arm, the
  container-only scope of Search's object branch and its Intelligence-based trap
  assessment, the corpse split, the wall-torch borrow and the three laden-table
  eat arms with their unequal tails were re-derived and re-executed from private
  analysis in `u5-decomp/notes/`, `u5-decomp/functions/SJOG_OVL/` and
  `u5-decomp/functions/TOWN_OVL/`, with the stored text read back from the
  shipped data file.
